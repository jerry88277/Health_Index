# Devlog 2026-06-30

## 規劃 — CSTR 做/不做深度分析 + [參數×段×統計] 混合特徵層（v2 設計）
回應使用者「X&Y 健康指標展開到連續製程 + AVM [參數×製程步驟×統計] 套用連續製程」架構問題。

- **CSTR 研究（5 子代理 workflow）**：CSTR 無 recipe step，最佳對應 `D within B`（SSD 穩態 pseudo-run within
  mode/grade）。X 側 OSS（pmPCA/menelaus/frouros/alibi-detect）、Y 側軟測量退化（Kaneko/Funatsu 等，**多數
  付費牆 NOT VERIFIED**）、特徵工程實證『連續製程主流是 raw/lagged DPCA、非逐段統計；逐變數段統計丟跨變數相關＝
  本專案 covert drift 反面』。結論：**混合**——raw 多變量主偵測器不動，[參數×段×統計] 當 L4/可解釋疊加。
- **設計 v2（3 視角獨立紅隊 approve_with_changes）**：採全部 5 blocker——B1 偏離 z 改 block-bootstrap null
  （跨段 std 在 trim 下 1 段分母未定義）；B2 新欄 `seg_index` 非 RUN_ID；B3 參數 `method` 非 `mode`（撞保留欄）；
  B4「TEP 6 模式」事實錯→3 grade×N campaign（切分鍵 CAMPAIGN_ID）；B5 SSD 復用既有 _ramp/_seg_std+PELT、不另造門檻。
  wavelet 延後增量（PyWavelets 已裝備）。定位降級為『可解釋視圖、非偵測器』。

## 14:36 +0800 — refactor：抽 seg_ramp/seg_std 共用穩態準則（e300d6d）
特徵層 SSD 須與 golden 自動挑選用同一已校準穩態準則（紅隊 B5：不另造未校準門檻）。把
`dataframe._auto_select_golden` 內 nested `_ramp`/`_seg_std` 抽成 `preprocess/segment.py` 模組級
`seg_ramp`/`seg_std`，dataframe 改 import 復用。純行為保持重構（公式不變）。全套 401 passed。

## 14:44 +0800 — feat：特徵建構層 trim/ssd + block-bootstrap 偏離視圖（05d754e）
混合特徵層最小切片（附加層、不動骨架）：
- `detect_steady_segments`：trim（預設零調參）/ ssd（PELT+seg_ramp/seg_std）；wavelet 延後（未知 method raise）。
- `segment_statistics`：[param×stat] 6 統計；count→段級 metadata、mode 連續退化拒入網格。
- `segment_drift_view`：golden window-vs-window block-bootstrap null（block-aware、golden 1 段仍可定義）+
  Holm 校正 + chance_band + is_advisory；凍結感測器/段過短 → NaN+warn。
- WHY 測試 11 含 **marquee 硬閘**：純多變量飄移→段統計 Holm 0 顯著(盲)、L2 SPE 升 5×——鎖死『不可升級為偵測器』。

全套 412 passed（401+11）、型別 py_compile OK。第二波後端隔離項由背景 worktree agent 並行推。
待辦：demo 獨立 endpoint + 前端兩 step（gate3/4→step）、wavelet 增量、`docs/redteam_verified_issues.md`/
`system_issues_review.md` 仍未追蹤（背景 agent worktree 讀不到，待 commit 入庫）。

## 14:57 +0800 — 第二波後端隔離項：ROI A17 + acceptance A10（929e77f, ab331f6）
背景 worktree agent 並行偵測，但其 worktree 從 `f01798e`（Wave-1 前）切出 → events A1/A18 重做（**main
已有 `_parse_aware`/opened_at，丟棄**），roi/acceptance 為 main 現版活 bug、由我**手動重套到 main 現版**。
- **roi A17（929e77f）**：分母 `!= 'false_alarm'` → `== 'real'`。原計入 open(未確認自動告警)/ignore →
  ROI 由『未確認告警數×自填損失×0.5』虛灌（結合 A1 更嚴重）。test_roi.py 4 WHY + 對齊 test_p2_backend。
- **acceptance A10（ab331f6）**：使用者反覆改 golden 段過 FPR gate＝p-hacking，報告卻當 out-of-sample 估計。
  Option-A 誠實揭露（非硬擋）：AcceptanceReport 加 golden_selected_by_user/selection_disclosure、verdict
  PASS/FAIL 皆附揭露；手動套時保留 Wave-1 的 y_fpr_ok。test_acceptance 3 WHY。

全套 419 passed。worktree agent 分支不整批 merge（基底脫鉤）→ 改手動重套後刪 worktree。
協調教訓：背景 worktree 須確認基底＝當前 HEAD，否則重做已存在的修復；下次 spawn 前先確認 git HEAD 一致。
