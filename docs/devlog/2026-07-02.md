# Devlog 2026-07-02

（承 2026-07-01 batch-AVM/Golden 設計規劃；本日進入設計文件回填 + TDD 實作。git 時間為權威。）

## 10:03 +0800 — CV+/jackknife+ 小 n 可信度（92bb592，TDD-0，[verified]）
紅隊 must-fix #1/#2 落地。golden 單 campaign n<cp_min_calibration(200) → split-CP 退回 GSI、Ŷ 失可信區間。
新增 `detectors/conformal_cv.py::CVPlusConformal`：K-fold leave-fold-out，每點 both fit both 校準，
**自有門檻 `cv_plus_min_obs`（獨立於 200）**故小 n 可用；`n_folds≥n`→jackknife+。
- 覆蓋 worst-case **≥1−2α（0.80@α=0.1）**，`coverage_floor` 誠實回報，**不**沿用 split-CP 的 ≥1−α（A11）；
  非抗漂移（re-entry 破 exchangeability 對兩者一視同仁，CV+ 只買小 n 可用性）。
- 確定性（折按 index 取模，無 RNG，Rule 5）；**不動** SoftSensor/PLSSoftSensor 的 split-CP 契約（Rule 3）。
- config 加 additive 欄位 `cv_plus_folds=5`、`cv_plus_min_obs=20`。
- TDD 紅→綠：先寫 6 WHY 測試（marquee＝「200 門檻不得管轄 CV+」）見 ModuleNotFoundError 為 RED，
  實作後 6/6 綠、**全套 445 passed（+6，零 regression）**、py_compile OK。
- NOT VERIFIED：Barber et al. 2021（jackknife+，Ann. Statist. 49(1):486-507）不在 crossref，待 VERIFY 方可作已驗證引用。

## 10:03 +0800 — 回填 batch-AVM 設計文件（a9f7b94）
`docs/batch_avm_design.md`：整合紅隊 go_with_fixes（wf_a8bb680b，3 視角）全部 must-fix + DQIy 網路深查
（wf_a39e9f45）裁決落成版本化真相。要點：新增可加性 batch-AVM 路徑（X*=[param×stat]/批）、五維骨架不動；
machine_id→RESERVED（僅 provenance）；Golden 兩關卡（疊圖 trim+勾選 / indicator run 圖惰性計算）；
min/max/range 修訂（連續製程 n 近常數→保留+ n-guard，並雙用途當 sensor-health）；映射模型 GPR+PLS+CP 零新；
隱性 Y 漂移＝殘差 EWMA/CUSUM（砍 ART2 DQIy，DQIx-only）；池化同質性閘（build 時、between-cell、WARN）。
標 NOT VERIFIED / crossref TODO，含 ⚠ DQIy DOI 衝突（2154910 vs 現有 2146006，須上 IEEE Xplore 核對）。
