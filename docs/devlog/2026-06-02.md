# Devlog 2026-06-02

## 10:02:51 +0800 · commit `72a4173`
**設計文件 v0.2（納入三方紅隊對帳）+ 審核獨立性規則與 hook**

- **現代化調查**：4 份 `modernization_*`（L1–L4 經典→現代候選）+ `modernization_map`（5W1H 升級地圖）。
- **自我審核**：`modernization_audit`（F1–F3 事實 / H1–H8 概念），查證 RI DOI、Sinkhorn、Energy=MMD。
- **獨立紅隊（解球員兼裁判）**：3 份 `redteam_*`（統計/落地/文獻）+ `redteam_reconciliation`（三方對帳）。紅隊抓出自審「6/11 修對一半」+ 14 個雙方盲點，硬傷 3：N1 功效公式張冠李戴、N2 全鏈無 FWER（破壞 AC-1）、H7 確定性誤判。
- **設計回填 v0.2**：requirements/functional/development 三份納入——FWER 單一決策點、KS first-pass→MMD、解析 1D-Wasserstein、ICAD 免標籤可信度、CP 最小 calibration 門檻、RBC caveat（多方向殘留 smearing）、block-permutation+凍結模型、null 標準化、TEP power curve、A/B 綁真實集不退化、單一 config 超參治理、降級階梯、RNG seed+容忍帶。
- **治理**：`CLAUDE.md` 新增「審核獨立性」規則；`.claude/` PreToolUse hook 於 `git commit` 提醒派獨立紅隊（已測：git commit 觸發、git status 靜默、settings.json 合法）。
- **文獻修正**：RI DOI `.914388`→`.914373`（前者指向他人論文，危險錯引）；Spatio-temporal LSTM 2023 期刊 *Control Eng. Pract.*→*Eng. Appl. AI*（DOI 10.1016/j.engappai.2023.106847）。

**狀態**：規劃與設計階段收斂。

## 10:20:40 +0800 · commit `3c8918c` · tag `green-20260602-1005`
**M0 專案骨架 + interface 雙軌契約 + config 單一超參 [verified]**

- `pyproject.toml`/`requirements.txt`：核心科學棧（numpy/scipy/sklearn/pandas/POT/pytest）；API/UI 依賴延後至 M7/M8（Rule 2）。
- `src/health_index/`：`__init__`（版本+Rule 5/3 註）、`config.py`（單一超參真相，含紅隊衍生參數 min_samples_per_dim/cp_min_calibration/drift_persistence_k/fwer_method）、`interface.py`（雙軌契約：raw/derived/Mode + validate_raw，骨架穩定 Rule 3）。
- `tests/test_smoke.py`：7 測試編碼 WHY（契約是骨架、擋壞資料）。
- `.gitattributes` 統一 LF、`.gitignore` 排除 `.venv`/`data`。
- **驗證**：`pip install -e .[dev]` OK、`pytest` 7 passed、`py_compile` OK、import OK。
- 環境：`.venv`（--system-site-packages，沿用系統 numpy 2.4.4/pandas 3.0.2/scipy/sklearn）。

**狀態**：M0 完成並綠燈。

## M1 unit-1 · commit `a8d1dfa` · tag `green-20260602-m1u1`
**合成連續製程生成器 + WHY 測試**（迴圈 /loop 10m 自動推進）

- `adapters/synthetic.py`：確定性生成 grade A→B→A→C→A；re-entry A 段注入隱性多變量飄移（擾動載荷方向、逐列重正規化保各變數變異）→ 單變數不可見、多變量可見。
- 決策（Rule 1/2）：用純 numpy 合成而非 pyTEP（後者需 MATLAB/Fortran，地端難即裝）；ground-truth 完全可控，真實 adapter 日後接同一契約。
- `tests/test_synthetic.py`：5 WHY 測試。
- **獨立子代理 code review（審核獨立性規則）抓到假綠 bug**：`fro_drift>0.5` 落在 n=300 噪聲地板（drift=0 也過）→ 改 `>2.0` + `>3×clean`、收緊 mean(<0.3σ)/std 門檻。
- 驗證：pytest 12 passed、py_compile OK。
- 迴圈四環節（coding→test→review→push）首次完整跑通。

**下一步**：M1 unit-2。

## M1 unit-2 · commit `423ca84` · tag `green-20260602-m1u2`
**preprocess 分段：campaign/mode/run_id/golden-A + ruptures 變點**

- `preprocess/segment.py`：`add_campaign_ids`（grade 連續段）、`detect_change_points`（ruptures PELT/Dynp rbf，label-agnostic，預設接 config.ssd_penalty）、`segment`（填衍生欄；golden-A 僅第一段穩態 A、排除 transition settling 與 re-entry/drift 段）。
- 裝 `ruptures v1.1.10`，加進 pyproject/requirements。
- **2 個獨立子代理 code review（≥2 視角）**：B 抓到 A 漏掉的 🔴（ruptures 未列依賴→他機 CI 失敗）+ 🔴（penalty 未接 config）；皆修。transition 溢出 A 判 bug、我 audit 後判 no-op，仍套 clamp。
- 驗證：pytest 18 passed、py_compile OK。

**下一步**：M1 unit-3 收尾 M1。

## M1 unit-3 · commit `cbc34eb` · tag `green-20260602-m1u3`
**X→Y 延遲對齊（align）**

- `preprocess/align.py`：`estimate_delay`（對齊後 X→Y 線性 R² 最高的延遲）、`add_y_delay`（寫 Y_DELAY 衍生欄）。
- **2 個獨立 reviewer（≥2 視角）**：A 抓到自審漏掉的**數值 bug**——跨 lag 比 raw R² 有 selection bias（樣本數不等→偽高）+ 門檻過鬆 overfit（max_lag=1300 回 1252）；修為「等樣本子集比較 + ≥2p+1 殘差自由度」。B 抓到型別註解錯。
- 補對抗測試（大 max_lag 不選偽延遲、estimate→寫欄接縫）、pyproject 加 pytest pythonpath（償還 M0 債）。
- 驗證：pytest 24 passed、py_compile OK、max_lag=1300→0 直接驗證。

### ✅ M1 核心完成（合成資料 + 分段 + 對齊）
- 已可在完全可控的 ground-truth 上開發/驗證偵測鏈（AC-1/2/3 所需）。
- **Backlog（surface）**：真實 TEP/PRONTO/Gas adapter 需下載資料（網路/授權），列為資料就緒後的單元，不阻塞偵測鏈開發。

**下一步**：M2。

## M2 · commit `5557f5a` · tag `green-20260602-m2`
**L1 DQI_x 資料效度閘（sanity + FastMCD + DQI_x）**

- `detectors/dqi_x.py`：`sanity_check`（NaN/超界/凍結）+ `DQIxGate`（FastMCD robust 中心/協方差 + PCA k 維 Euclidean DQI_x，門檻 MCD support 內 trimmed mean）。
- **2 個獨立 reviewer 獨立收斂到同一 🔴**：FastMCD 對比測試假綠（`MCD→np.cov` 全綠）+ 底層 production bug（門檻全點 in-sample trim + sample-std 標準化，污染率>trim 時門檻被吹大／std 膨脹壓縮離群，mag=6 漏 73% 離群）。
- 修：門檻改 MCD support 內點、標準化改 **median/MAD**（robust 全鏈）；測試改 **robust-vs-sample-cov 直接對比**（殺 mutant，robust 標出 98% vs naive 76%）。config 註解對齊 in-sample 偏離（Rule 12）。
- 誠實層分離 WHY：grade B 正交位移 DQI_x 盲、殘差(SPE)抓到 → 動機 L2。
- 驗證：pytest 35 passed、py_compile OK。

**下一步**：M3。

## M3 · commit `b124aa9` · tag `green-20260602-m3`
**L2 MSPC 核心：PCA → T²/SPE/GSI + 經驗控制限 + RBC**

- `detectors/mspc.py`：`MSPCModel`（T²=Σtᵢ²/λᵢ、SPE=殘差、GSI=D²/p、經驗(1−α)控制限、RBC=resid²/C̃_jj）。
- **marquee WHY 成立**：純多變量隱性飄移被 SPE 抓、被逐變數 3σ 圖漏（＝成功判準 2、index 存在的理由）。
- **2 個獨立 reviewer**：A 驗證 5 公式機器精度全對（≤1e-14，含 RBC vs Alcala&Qin）；B **跨 40-seed 掃描**抓到 2 個 🔴 測試嚴謹度問題——🔴-1 marquee 是 seed 巧合（spe_detect>0.5 在 4/40 失敗）→ 改 parametrize+容忍帶+drift_strength=1.5+t2 專屬性；🔴-2 golden FA near-tautological（in-sample~2α 恆過、hold-out~4α）→ 誠實降級不再宣稱型一受控。
- backlog task：型一 hold-out/block-bootstrap 控制、GSI floor 近共線放大、k 邊界。
- 驗證：pytest 45 passed、py_compile OK。

**下一步**：M4。

## M4 · commit `218c65f` · tag `green-20260602-m4`
**L3 軟測量：GPR Ŷ + split-Conformal 取代 RI**

- `detectors/soft_sensor.py`：GPR 預測 Ŷ（確定性）+ split-CP 預測區間（取代 AVM RI，有限樣本覆蓋保證）+ cp_min_calibration 閘（標籤稀少→退回 GSI，H1 雙路）。
- **marquee WHY**：CP 覆蓋率 ≈ 1−α（5 seed 實測 0.90 校準、區間有限寬）。
- **2 個獨立 reviewer**，對帳衝突（Rule 7）：A 抓 🔴 R-1（B 判 0🔴）→ 採 A。R-1＝`min(level,1.0)` 在小 n/小 α 靜默把 CP 分位 clip 成 max(scores)、違反覆蓋保證 → 改精確序統計 + k>n 退回 GSI。
- 設計忠實 H1：只做有標籤 CP、無標籤交上層 GSI；時序 CP 不宣稱保證。
- 驗證：pytest 51 passed、py_compile OK。

**下一步**：M5。

## M5 · commit `a3132b4` · tag `green-20260602-m5`
**L4 campaign 分佈漂移：KS→MMD + 1D-Wasserstein + PSI**

- `detectors/drift.py`：PCA 分數空間的 KS first-pass→MMD 分層、解析 1D-Wasserstein 量級（對 null 標準化）、PSI（僅溝通）、block-permutation。
- **marquee WHY（判準 3）**：殘留 drift 的 A 被判漂移、乾淨換線回歸的 A 不被判（分數空間捕捉關係型隱性漂移）。
- **2 個獨立 reviewer 各抓不同真問題**：🔴 A＝Wasserstein null 樣本數不一致→z 隨段長爆走會誤報健康段（威脅 DoD#1）→改 s-vs-s 等樣本 disjoint 抽樣；🔴 B＝marquee 殺不掉旁路 KS/MMD/OR mutation→補分層 AND monkeypatch（含成本分層）；R2 vacuous 斷言→改 abs。
- 驗證：pytest 64 passed、py_compile OK。

### 判斷鏈四層全部就位
L1 效度閘 ✅ → L2 多變量域 ✅ → L3 軟測量可信度 ✅ → L4 campaign 漂移 ✅。

**下一步（dev plan v0.2）**：**M6 Health Index 融合 + re-entry 觸發**（各層→對 null 尾機率→加權 0–1；單一決策點+FWER；非 A campaign/維修後第一段 A 的 re-entry 偵測）。
> 註：L5 批次 DTW 依 v0.2 **延後出 MVP**（連續型聚焦），待 penicillin 批次資料就緒再做。

## M6 · commit `e75b282` · tag `green-20260602-m6`
**Health Index 融合 + re-entry 觸發（判斷鏈收口）**

- `health.py`：L1/L2/L4 → 0–1 子分數（高=健康）→ 加權 Health Index；`is_alarm`=融合趨勢+硬閘安全網雙軌（H8）；`detect_reentry_campaigns`（grade 切換型）。
- **marquee WHY**：HI 對 golden/乾淨回歸維持高分、對殘留 drift 掉到低分並告警（判準 1+3）。
- **2 個獨立 reviewer 強烈收斂（最徹底一次）**：production 邏輯正確、但測試嚴重給假信心。
  - 🔴 marquee 殺不掉旁路 L2/L4 mutation → 補逐層 ablation。
  - 🔴 判準 2（早於單變數 SPC）核心 WHY 完全沒測 → 補 SPC 對照（drift 越界率<0.1=盲、HI 告警）×5 seed。
  - 🔴 is_alarm OR 硬閘使告警測試不驗融合 → 補融合獨立鑑別 + H8 機制驗證。
  - 🔴 「單一決策點」vs OR 硬閘自相矛盾 → docstring 誠實改「雙軌」；FWER/持續性/維修 re-entry/N6 尾機率明標 M9/M-later 待辦。
- 誠實發現：合成資料無法自然產生「融合漏、硬閘獨抓」的 H8 case → 硬閘改機制性驗證。
- 驗證：pytest 81 passed、py_compile OK。

### 🎯 五維度 MECE 判斷鏈全部實作完畢
L1 效度閘 → L2 多變量域(T²/SPE/GSI) → L3 軟測量(GPR+CP) → L4 漂移(MMD/Wasserstein) → Health Index 融合 + re-entry。三條成功判準皆有 WHY 測試守護。

**下一步（dev plan v0.2）**：**M7 後端 API（FastAPI）** → M8 前端（Dash）→ M9 cross-validation 驗證 → M10 啟動手冊。

## M7 · commit `d849171` · tag `green-20260602-m7`
**FastAPI 後端：REST 封裝判斷鏈**

- `api/server.py` + `schemas.py`：GET /health、GET /datasets、POST /analyze（合成→分段→golden-A fit→per-campaign 健康度/告警/re-entry）。裝 `fastapi 0.136.3`。
- **API-level marquee**：判斷鏈經 HTTP 端到端區分 drift vs golden/clean + 告警 + re-entry=[2,4]。
- **2 個獨立 reviewer**：marquee 經 7-mutation 全殺；收斂 3 個 🔴——負 seed→未捕捉 500（→Field ge=0 變 422）、§5 端點缺口未誠實標（→docstring Rule 12 標記 /baseline /contribution /crossval 待辦）、importorskip 只守 fastapi 不守 httpx（→補守門 + httpx 入 dev）。
- 🟡 補 API 決定性測試、drift_strength 上界防 inf。薄封裝忠實（Rule 3，未複製偵測數學）。
- 驗證：pytest 88 passed、py_compile OK、uvicorn app 可載入。

**下一步（dev plan v0.2）**：**M8 前端（Plotly Dash）**——HI 時間軸/T²·SPE·GSI/RBC 肇因/降級標示，經 REST 取 /analyze 結果 → M9 cross-validation → M10 啟動手冊。

## M8 · commit `165e587` · tag `green-20260602-m8`
**Plotly Dash 前端：視覺化判斷鏈結果**

- `frontend/app.py`：經 REST 取後端 /analyze → `build_health_figure`（HI 長條，紅=告警/*=re-entry）、`build_subscore_figure`（L1/L2/L4）、Dash app（控制列+雙圖+狀態）。裝 dash 4.2/plotly 6.7。
- **marquee WHY**：HI 圖忠實反映鑑別（drift 紅/低、golden 綠/高），餵真後端真判斷鏈端到端。
- **2 個獨立 reviewer**：health 圖 marquee 經 5-6 mutation 全殺；A 判可 push、B 抓 🔴（Rule 7 取嚴）——子分數圖只驗 trace 名稱不驗值（L2↔L4 互換/全 0 假綠）→ 補鑑別斷言。
- 薄封裝忠實（Rule 3，零判斷鏈重算）；§6 per-sample 時間軸/Ŷvsy/RBC 誠實標 M-later（待後端端點）。
- 驗證：pytest 94 passed、py_compile OK。

### 🎯 全棧打通
判斷鏈（L1–L4+融合） → REST 後端（FastAPI） → 前端（Dash），端到端隱性飄移偵測 + 視覺化。

**下一步（dev plan v0.2）**：**M9 cross-validation 驗證**。
> ⚠️ 預期阻塞：M9 的 AC-4「≥2 資料集、真實集不退化」需下載真實 TEP/PRONTO/Gas（網路/授權）；目前僅合成資料。屆時會 surface 並與使用者確認（用合成多組態做泛化、或啟動真實資料下載）。

## M9 · commit `156769d` · tag `green-20260602-m9`
**cross-validation runner：跨組態超參 robustness（AC-1/2/3）+ 泛化報告**

- 接續上個 session（因 API 529 Overloaded 連環卡死於此處）：`crossval.py` 已寫好待補測試，本輪收尾。
- `validation/crossval.py`：固定 `HealthIndex` **零 per-config 調參** × 5 組多樣合成組態（不同 p/noise/drift/seed）→ `evaluate_configuration` 檢核 AC-1/2/3（golden 健康、隱性 drift 被抓且 3σ SPC 全盲、drift 明顯低於乾淨回歸）；`format_report` 匯出 markdown 泛化報告。實測 5 組全過（golden HI ~0.99、drift HI 0.29–0.43、SPC 盲 1–4%）。
- `tests/test_crossval.py`：10 條 WHY 測試。**mutant-killer**（drift_strength=0 → AC2/AC3 必 False，證 AC 非恆真假綠）、**邊際變異守恆**（鎖「SPC 為何該盲」根因，generator 破壞此不變式即紅）、grid 真多樣、all_pass=AND、零調參、決定論、報告誠實標 backlog。
- **2 獨立紅隊複審（≥2 視角：統計嚴謹度 / 範圍誠實）強烈收斂**：數學與測試誠信主幹紮實（mutant-killer 有效、SPC 閾值 0.1 有解析依據非 cherry-pick、邊界切片正確、決定論跨進程驗證、零調參確認）；共同抓出**唯一承載性風險＝過度宣稱**——🔴 測試名 `generalizes` 超出證據（實為「單一線性高斯 DGP 內超參 robustness」非跨 DGP 泛化）。**已修**：改名 `robust`、docstring/commit 精準降級、報告抬頭明標非完整 AC-4。
- **誠實殘項（Rule 12，未宣稱完成）**：(a) **完整 AC-4「真實集不退化」未做**＝正式採用門檻，需下載真實資料（UCI Gas Drift 為最低成本起點）；(b) 報告目前為函式匯出，未落地成固定報告檔。
- 驗證：pytest **104 passed**（M8 為 94，+10）、py_compile OK、crossval 直接執行 5/5 AC 全過。

**下一步（dev plan v0.2）**：**M10 啟動手冊（startup_manual.md）+ Demo walkthrough**——乾淨 venv 一次拉起全棧（後端 uvicorn + 前端 Dash + 健康檢查 + 煙霧測試 + FAQ）。
> 殘項回填：真實集 adapter（AC-4 正式門檻）與泛化報告檔可於 M10 附帶或另開單元。

## M10 · commit `7e5833b`(docs) + `f713fdf`(fix) · tag `green-20260602-m10`
**全棧啟動手冊 startup_manual.md + cp950 bug fix（里程碑收官）**

- `docs/startup_manual.md`：dev_plan §6 七要件齊備——venv → 資料（合成內建/真實 backlog）→ 後端 `uvicorn health_index.api.server:app --port 8000` → 前端 `python frontend/app.py`（:8050）→ 健康檢查 → 煙霧測試（pytest/curl/crossval）→ FAQ（埠/路徑/版本/降級）+ §7 Demo walkthrough + §8 範圍限制表。
- **DoD「乾淨環境一次拉起」實測驗證**：真實 uvicorn port 綁定 live `/health`+`/analyze` 皆 200、TestClient 端到端、format_report 一行命令；§7 表（C0/C2 健康、C4 殘留飄移 HI=0.288 告警、reentry=[2,4]、SPC 盲 1%）與 server.py 邏輯逐列一致。
- **cp950 bug（M10 驗證時抓到）**：`format_report` 原含 ∧/✅/❌（非 cp950），手冊文件化的 `print(format_report(...))` 在 Windows 預設主控台 `UnicodeEncodeError` → 改 Y/-/& ASCII 標記，補 `test_format_report_is_windows_console_safe` 回歸鎖死（fix `f713fdf`）。
- **2 獨立 reviewer（技術準確度 / 範圍誠實）逐條對照原始碼**：app 路徑/埠/版本/extras/schema 邊界/demo 表全一致；修過時測試數（104→105）、補 §8 AC-6/FWER 殘項。Reviewer「crossval 未入庫」🔴 為舊快照（已於 M9 `156769d` 追蹤）對帳釐清。
- **誠實殘項（Rule 12）**：§8 列全部已知缺口——完整 AC-4 真實集、/baseline·/contribution·時間軸端點、L5 批次 DTW、AC-6 嚴格 FWER、維修型 re-entry；不宣稱 AC-4 達成。
- 驗證：pytest **105 passed**（M9 為 104，+1 cp950 回歸）、py_compile OK、全棧 live 拉起驗證。

### 🎯 M0–M10 里程碑全部完成
骨架 → 資料層 → L1–L4 偵測鏈 → Health Index 融合/re-entry → 後端 API → 前端 Dash → cross-validation → 啟動手冊。三條成功判準皆有 WHY 測試守護，全棧端到端可拉起。
> **後續 backlog（非 MVP 阻塞，待使用者決策）**：真實集 adapter（AC-4 正式門檻，UCI Gas Drift 起點）、per-sample 時間軸/RBC 端點、L5 批次 DTW、AC-6 嚴格 FWER、維修型 re-entry。

## 16:54 · commit `08ef72a` · 規劃：backlog 文件化 + 啟動 B1–B4 開發迴圈
**`docs/backlog.md`：4 項缺口轉 repo 版本化真相**

- 對話示範 RBC 肇因（seed=5 drift 段：x01/x03/x07/x08 RBC 比乾淨段高 14–49×、而單變數 3σ 越界率 ~0 → 多變量點名、單變數全盲），確認「知道是什麼參數」現有偵測鏈已可算、缺 UI 接線。
- 文件化 B1 時間軸端點+RBC 上前端／B2 真實集 adapter／B3 AC-6 FWER／B4 L5 批次 DTW，各含 為何/現況/範圍誠實/DoD/工作量；優先序 B1→B4。
- 啟動 `/loop 10m` 從 **B1** 開始，每項走 coding→unit test→code review→git push→compact session 五環節，逐項至完畢。

**下一步**：B1 — 時間軸端點 + RBC 上前端。

## B1 · commit `57a62b1` · tag `green-20260602-b1`
**時間軸端點 + RBC 肇因上前端（指出何時飄移、哪個變數）[verified]**

- 後端薄封裝（零判斷鏈重算）：`POST /timeline`（逐樣本 T²/SPE/GSI + 控制限 + campaign 邊界）、`POST /contribution`（per-campaign RBC 肇因排序 + 單變數 3σ 越界率對照）；抽 `_prepare` 共用 helper，三端點共用同一凍結 MSPC 模型。
- 前端：時間軸圖（SPE/T² + 控制限線 + campaign 邊界）+ 肇因圖（選 campaign → RBC 變數降序，文字標 SPC 越界率）；create_app 接 contrib-campaign 下拉 + 2 圖。
- **marquee WHY**：drift campaign top-RBC 變數（x01/x07/x03）單變數 3σ 越界率近 0 → RBC 點名單變數 SPC 全盲的隱性飄移變數；timeline 顯示 drift 段 SPE 越限率 0.92 vs golden ~0.01（α 地板）。
- **2 獨立 reviewer（測試誠信 / 薄封裝誠實）**：A mutant 殺傷餘裕 5–14×、薄封裝保真測試鎖「API SPE==核心」、0🔴；B 薄封裝/無狀態/骨架穩定全 ✅，抓 **3🔴 誠實標記過時（反向不誠實：宣稱未做的已做）**——已修 server.py/frontend 頂部 docstring、端點命名偏離記入 functional_design §5 + startup_manual §8。另採 2🟡（弱化「grade 位移 SPC 可見」過度概括、補 `_campaign_spans` campaign 連續不變式）。
- **端點命名偏離（Rule 12）**：MVP 無狀態（請求帶 seed/drift 重算、無 job store）→ `POST /timeline`、`/contribution` 取代原訂 `GET /analyze/{job}/health`、`/contribution`；待引入 job store 再回 RESTful 子資源路徑。
- 驗證：pytest **115 passed**（M10 為 105，+10 B1）、py_compile OK、後端 live 兩端點 + 前端 build 實測。

**下一步**：B2 — 真實集 adapter（UCI Gas Drift 起點；⚠️ 需下載真實資料，屬預知阻塞，將先 surface 與使用者確認再動）。

## B2 · commit `e80313d` · tag `green-20260602-b2`
**UCI Gas Drift 真實集 adapter — AC-4 核心邏輯轉移驗證 [verified]**

- 使用者授權下載 UCI #224（~10MB）→ 解壓 `data/uci_gas_drift/Dataset`（gitignore 不入 repo）。
- `adapters/uci_gas_drift.py`：解析 libsvm（128 feat=16 sensor×8、6 氣體、10 batch 跨 36 月）→ 統一 `interface` 契約 + `GasDriftGroundTruth`。**語意映射明列（Rule 1）**：X=感測器特徵、grade=gas、campaign=batch、golden=batch1；drift=時間性感測器老化（非換線、數學形態一致）。golden 採 batch1 混合 6 氣體（單氣 batch1 僅 30–98 樣本 <2p）。
- `validation/real_set.py`：`evaluate_gas_drift` 以**核心 L2 MSPC SPE（零 per-dataset 調參）**檢核 **AC-4a golden hold-out 不退化（實測異常率 1.8%≈α）+ AC-4b 真實漂移被抓（batch3/4=0.40/0.69）**。
- **關鍵發現（Rule 6 可轉移性）**：p=128 時 **L1 FastMCD（fit 子集 n≈222<2p=256，MinCovDet 不穩/極慢）與 L4 全 permutation（大 batch O(B·n²)）超出線上成本**——全鏈未在 p=128 跑通；B2 以可轉移的 L2 MSPC 為證，全鏈 PCA-score 降級列 backlog，**不宣稱完整 AC-4 達成**。真實漂移**非單調**（batch8/9 低，資料集重校事件）→ 只斷言「存在被抓的漂移 batch」，不造假單調。
- **2 獨立紅隊（統計可轉移性 / 資料語意誠實）皆 0🔴**：A 確認漂移跨 5 seed 由 batch3/4/5 穩定支撐（非 cherry-pick）、3 mutant 全殺、降級方向皆偏保守不製造假陽；B 確認語意映射對照 UCI #224 官方全正確、非單調/無Y/降級三處主動標明。採 4 個 🟡：修 n<2p 口徑（golden 全量 vs fit 子集）、補 seed 反向 assert、混合氣體取捨說明、`literature_crossref.md` 補 Gas Drift 指向 survey。
- 驗證：pytest **122 passed**（B1 為 115，+7）、py_compile OK、AC-4a/4b 實測通過。

**B2 殘項（backlog）**：全鏈 L1/L4 的 PCA-score 降級（讓完整 Health Index 在 p=128 可跑）、RealSetResult 多 seed 變異帶（split 變異統計嚴謹度）。

**下一步**：B3 — AC-6 FWER（golden-A 誤報率≤α；純數學、不需下載，**未阻塞**）。
