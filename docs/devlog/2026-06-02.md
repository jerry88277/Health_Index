# Devlog 2026-06-02

## 10:02:51 +0800 · commit `72a4173`
**設計文件 v0.2（納入三方紅隊對帳）+ 審核獨立性規則與 hook**

- **現代化調查**：4 份 `modernization_*`（L1–L4 經典→現代候選）+ `modernization_map`（5W1H 升級地圖）。
- **自我審核**：`modernization_audit`（F1–F3 事實 / H1–H8 概念），查證 RI DOI、Sinkhorn、Energy=MMD。
- **獨立紅隊（解球員兼裁判）**：3 份 `redteam_*`（統計/落地/文獻）+ `redteam_reconciliation`（三方對帳）。紅隊抓出自審「6/11 修對一半」+ 14 個雙方盲點，硬傷 3：N1 功效公式張冠李戴、N2 全鏈無 FWER（破壞 AC-1）、H7 確定性誤判。
- **設計回填 v0.2**：requirements/functional/development 三份納入——FWER 單一決策點、KS first-pass→MMD、解析 1D-Wasserstein、ICAD 免標籤可信度、CP 最小 calibration 門檻、RBC caveat（多方向殘留 smearing）、block-permutation+凍結模型、null 標準化、TEP power curve、A/B 綁真實集不退化、單一 config 超參治理、降級階梯、RNG seed+容忍帶。
- **治理**：`CLAUDE.md` 新增「審核獨立性」規則；`.claude/` PreToolUse hook 於 `git commit` 提醒派獨立紅隊（已測：git commit 觸發、git status 靜默、settings.json 合法）。
- **文獻修正**：RI DOI `.914388`→`.914373`（前者指向他人論文，危險錯引）；Spatio-temporal LSTM 2023 期刊 *Control Eng. Pract.*→*Eng. Appl. AI*（DOI 10.1016/j.engappai.2023.106847）。

**狀態**：規劃與設計階段收斂。

## 10:20:40 +0800 · commit `3c8918c` · tag `green-20260602-1005`
**M0 專案骨架 + interface 雙軌契約 + config 單一超參 [verified]**

- `pyproject.toml`/`requirements.txt`：核心科學棧（numpy/scipy/sklearn/pandas/POT/pytest）；API/UI 依賴延後至 M7/M8（Rule 2）。
- `src/health_index/`：`__init__`（版本+Rule 5/3 註）、`config.py`（單一超參真相，含紅隊衍生參數 min_samples_per_dim/cp_min_calibration/drift_persistence_k/fwer_method）、`interface.py`（雙軌契約：raw/derived/Mode + validate_raw，骨架穩定 Rule 3）。
- `tests/test_smoke.py`：7 測試編碼 WHY（契約是骨架、擋壞資料）。
- `.gitattributes` 統一 LF、`.gitignore` 排除 `.venv`/`data`。
- **驗證**：`pip install -e .[dev]` OK、`pytest` 7 passed、`py_compile` OK、import OK。
- 環境：`.venv`（--system-site-packages，沿用系統 numpy 2.4.4/pandas 3.0.2/scipy/sklearn）。

**狀態**：M0 完成並綠燈。

## M1 unit-1 · commit `a8d1dfa` · tag `green-20260602-m1u1`
**合成連續製程生成器 + WHY 測試**（迴圈 /loop 10m 自動推進）

- `adapters/synthetic.py`：確定性生成 grade A→B→A→C→A；re-entry A 段注入隱性多變量飄移（擾動載荷方向、逐列重正規化保各變數變異）→ 單變數不可見、多變量可見。
- 決策（Rule 1/2）：用純 numpy 合成而非 pyTEP（後者需 MATLAB/Fortran，地端難即裝）；ground-truth 完全可控，真實 adapter 日後接同一契約。
- `tests/test_synthetic.py`：5 WHY 測試。
- **獨立子代理 code review（審核獨立性規則）抓到假綠 bug**：`fro_drift>0.5` 落在 n=300 噪聲地板（drift=0 也過）→ 改 `>2.0` + `>3×clean`、收緊 mean(<0.3σ)/std 門檻。
- 驗證：pytest 12 passed、py_compile OK。
- 迴圈四環節（coding→test→review→push）首次完整跑通。

**下一步**：M1 unit-2。

## M1 unit-2 · commit `423ca84` · tag `green-20260602-m1u2`
**preprocess 分段：campaign/mode/run_id/golden-A + ruptures 變點**

- `preprocess/segment.py`：`add_campaign_ids`（grade 連續段）、`detect_change_points`（ruptures PELT/Dynp rbf，label-agnostic，預設接 config.ssd_penalty）、`segment`（填衍生欄；golden-A 僅第一段穩態 A、排除 transition settling 與 re-entry/drift 段）。
- 裝 `ruptures v1.1.10`，加進 pyproject/requirements。
- **2 個獨立子代理 code review（≥2 視角）**：B 抓到 A 漏掉的 🔴（ruptures 未列依賴→他機 CI 失敗）+ 🔴（penalty 未接 config）；皆修。transition 溢出 A 判 bug、我 audit 後判 no-op，仍套 clamp。
- 驗證：pytest 18 passed、py_compile OK。

**下一步**：M1 unit-3 收尾 M1。

## M1 unit-3 · commit `cbc34eb` · tag `green-20260602-m1u3`
**X→Y 延遲對齊（align）**

- `preprocess/align.py`：`estimate_delay`（對齊後 X→Y 線性 R² 最高的延遲）、`add_y_delay`（寫 Y_DELAY 衍生欄）。
- **2 個獨立 reviewer（≥2 視角）**：A 抓到自審漏掉的**數值 bug**——跨 lag 比 raw R² 有 selection bias（樣本數不等→偽高）+ 門檻過鬆 overfit（max_lag=1300 回 1252）；修為「等樣本子集比較 + ≥2p+1 殘差自由度」。B 抓到型別註解錯。
- 補對抗測試（大 max_lag 不選偽延遲、estimate→寫欄接縫）、pyproject 加 pytest pythonpath（償還 M0 債）。
- 驗證：pytest 24 passed、py_compile OK、max_lag=1300→0 直接驗證。

### ✅ M1 核心完成（合成資料 + 分段 + 對齊）
- 已可在完全可控的 ground-truth 上開發/驗證偵測鏈（AC-1/2/3 所需）。
- **Backlog（surface）**：真實 TEP/PRONTO/Gas adapter 需下載資料（網路/授權），列為資料就緒後的單元，不阻塞偵測鏈開發。

**下一步**：M2。

## M2 · commit `5557f5a` · tag `green-20260602-m2`
**L1 DQI_x 資料效度閘（sanity + FastMCD + DQI_x）**

- `detectors/dqi_x.py`：`sanity_check`（NaN/超界/凍結）+ `DQIxGate`（FastMCD robust 中心/協方差 + PCA k 維 Euclidean DQI_x，門檻 MCD support 內 trimmed mean）。
- **2 個獨立 reviewer 獨立收斂到同一 🔴**：FastMCD 對比測試假綠（`MCD→np.cov` 全綠）+ 底層 production bug（門檻全點 in-sample trim + sample-std 標準化，污染率>trim 時門檻被吹大／std 膨脹壓縮離群，mag=6 漏 73% 離群）。
- 修：門檻改 MCD support 內點、標準化改 **median/MAD**（robust 全鏈）；測試改 **robust-vs-sample-cov 直接對比**（殺 mutant，robust 標出 98% vs naive 76%）。config 註解對齊 in-sample 偏離（Rule 12）。
- 誠實層分離 WHY：grade B 正交位移 DQI_x 盲、殘差(SPE)抓到 → 動機 L2。
- 驗證：pytest 35 passed、py_compile OK。

**下一步**：M3。

## M3 · commit `b124aa9` · tag `green-20260602-m3`
**L2 MSPC 核心：PCA → T²/SPE/GSI + 經驗控制限 + RBC**

- `detectors/mspc.py`：`MSPCModel`（T²=Σtᵢ²/λᵢ、SPE=殘差、GSI=D²/p、經驗(1−α)控制限、RBC=resid²/C̃_jj）。
- **marquee WHY 成立**：純多變量隱性飄移被 SPE 抓、被逐變數 3σ 圖漏（＝成功判準 2、index 存在的理由）。
- **2 個獨立 reviewer**：A 驗證 5 公式機器精度全對（≤1e-14，含 RBC vs Alcala&Qin）；B **跨 40-seed 掃描**抓到 2 個 🔴 測試嚴謹度問題——🔴-1 marquee 是 seed 巧合（spe_detect>0.5 在 4/40 失敗）→ 改 parametrize+容忍帶+drift_strength=1.5+t2 專屬性；🔴-2 golden FA near-tautological（in-sample~2α 恆過、hold-out~4α）→ 誠實降級不再宣稱型一受控。
- backlog task：型一 hold-out/block-bootstrap 控制、GSI floor 近共線放大、k 邊界。
- 驗證：pytest 45 passed、py_compile OK。

**下一步**：M4。

## M4 · commit `218c65f` · tag `green-20260602-m4`
**L3 軟測量：GPR Ŷ + split-Conformal 取代 RI**

- `detectors/soft_sensor.py`：GPR 預測 Ŷ（確定性）+ split-CP 預測區間（取代 AVM RI，有限樣本覆蓋保證）+ cp_min_calibration 閘（標籤稀少→退回 GSI，H1 雙路）。
- **marquee WHY**：CP 覆蓋率 ≈ 1−α（5 seed 實測 0.90 校準、區間有限寬）。
- **2 個獨立 reviewer**，對帳衝突（Rule 7）：A 抓 🔴 R-1（B 判 0🔴）→ 採 A。R-1＝`min(level,1.0)` 在小 n/小 α 靜默把 CP 分位 clip 成 max(scores)、違反覆蓋保證 → 改精確序統計 + k>n 退回 GSI。
- 設計忠實 H1：只做有標籤 CP、無標籤交上層 GSI；時序 CP 不宣稱保證。
- 驗證：pytest 51 passed、py_compile OK。

**下一步**：M5。

## M5 · commit `a3132b4` · tag `green-20260602-m5`
**L4 campaign 分佈漂移：KS→MMD + 1D-Wasserstein + PSI**

- `detectors/drift.py`：PCA 分數空間的 KS first-pass→MMD 分層、解析 1D-Wasserstein 量級（對 null 標準化）、PSI（僅溝通）、block-permutation。
- **marquee WHY（判準 3）**：殘留 drift 的 A 被判漂移、乾淨換線回歸的 A 不被判（分數空間捕捉關係型隱性漂移）。
- **2 個獨立 reviewer 各抓不同真問題**：🔴 A＝Wasserstein null 樣本數不一致→z 隨段長爆走會誤報健康段（威脅 DoD#1）→改 s-vs-s 等樣本 disjoint 抽樣；🔴 B＝marquee 殺不掉旁路 KS/MMD/OR mutation→補分層 AND monkeypatch（含成本分層）；R2 vacuous 斷言→改 abs。
- 驗證：pytest 64 passed、py_compile OK。

### 判斷鏈四層全部就位
L1 效度閘 ✅ → L2 多變量域 ✅ → L3 軟測量可信度 ✅ → L4 campaign 漂移 ✅。

**下一步（dev plan v0.2）**：**M6 Health Index 融合 + re-entry 觸發**（各層→對 null 尾機率→加權 0–1；單一決策點+FWER；非 A campaign/維修後第一段 A 的 re-entry 偵測）。
> 註：L5 批次 DTW 依 v0.2 **延後出 MVP**（連續型聚焦），待 penicillin 批次資料就緒再做。

## M6 · commit `e75b282` · tag `green-20260602-m6`
**Health Index 融合 + re-entry 觸發（判斷鏈收口）**

- `health.py`：L1/L2/L4 → 0–1 子分數（高=健康）→ 加權 Health Index；`is_alarm`=融合趨勢+硬閘安全網雙軌（H8）；`detect_reentry_campaigns`（grade 切換型）。
- **marquee WHY**：HI 對 golden/乾淨回歸維持高分、對殘留 drift 掉到低分並告警（判準 1+3）。
- **2 個獨立 reviewer 強烈收斂（最徹底一次）**：production 邏輯正確、但測試嚴重給假信心。
  - 🔴 marquee 殺不掉旁路 L2/L4 mutation → 補逐層 ablation。
  - 🔴 判準 2（早於單變數 SPC）核心 WHY 完全沒測 → 補 SPC 對照（drift 越界率<0.1=盲、HI 告警）×5 seed。
  - 🔴 is_alarm OR 硬閘使告警測試不驗融合 → 補融合獨立鑑別 + H8 機制驗證。
  - 🔴 「單一決策點」vs OR 硬閘自相矛盾 → docstring 誠實改「雙軌」；FWER/持續性/維修 re-entry/N6 尾機率明標 M9/M-later 待辦。
- 誠實發現：合成資料無法自然產生「融合漏、硬閘獨抓」的 H8 case → 硬閘改機制性驗證。
- 驗證：pytest 81 passed、py_compile OK。

### 🎯 五維度 MECE 判斷鏈全部實作完畢
L1 效度閘 → L2 多變量域(T²/SPE/GSI) → L3 軟測量(GPR+CP) → L4 漂移(MMD/Wasserstein) → Health Index 融合 + re-entry。三條成功判準皆有 WHY 測試守護。

**下一步（dev plan v0.2）**：**M7 後端 API（FastAPI）** → M8 前端（Dash）→ M9 cross-validation 驗證 → M10 啟動手冊。
