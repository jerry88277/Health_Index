# Devlog 2026-06-22

## 09:07 +0800 — 增量8 Y 側品質飄移預警維度（a141082）
回應使用者「品質標的(Y)出現隱性飄移時系統要預警，防量產次級品/報廢，算多一個維度監控」。

設計：複用既有 `YHealthIndex`（map/dist/y_flagged，建模時已 fit 進 bundle），接成獨立監控維度 + 事件流。
兩條品質飄移路（第一性原理）：① Ŷ 水準偏移（預測品質飄，3σ，新增 `config.y_trend_z_max`）；
② X→Y 殘差超界（真隱性：X 正常、實際 Y 偏，X→Y 關係斷）——單變數 SPC 抓不到，本維度存在理由。

落地：
- `score_timeline` 每窗加 `yhat_drift_z/y_map_health/y_flagged/y_observed/y_quality_raw/y_quality_persisted`
  + 頂層 `n_quality_alarms/golden_yhat_mean/std`；`window_detail` soft_sensor 加 `y_flagged`；CSV 加 `kind`。
- `events.py`：`Incident` 加 `kind`(process/quality)，`open_incident` 依 (product,kind) 防重複——品質與製程事件分流不互壓。
- 前端：ymap 品質飄移 ✕ 標記 + golden Ŷ 基準線、③品質維度狀態列（Y 稀疏誠實註記）、持續品質告警→開 quality 事件、
  下鑽顯品質旗標、事件卡 品質/製程 標籤。

誠實邊界（Rule 12）：②僅在 Y 樣本到達的窗可判——實測 synthetic Y 僅 5%（75/1500），window=60 下 0 窗有 ≥5 Y 觀測，
殘差無從評；窗觀測不足→`map_health=None`/`y_flagged=False`/UI 標「待量測」，不假綠。dense-Y 真實集（CCPP/Steel）
才是②的演示場景。

紅隊/驗證：先讀 `y_health.py` 確認原語（不憑記憶）；先實測 L4 單欄會崩（p≥2 假設）才據實答；TDD `test_y_quality.py`
（隱性品質飄移→告警、Y 稀疏→不假綠、無 Y→無維度）+ events kind 分流測試；test_demo/test_assets_demo/test_p2_backend
全通過、demo_app import 26 callbacks、`_run` 品質路徑 smoke（✕ 標記 + 開 quality 事件，X 正常時只開品質事件）。

備註：本回合中途 Bash 安全分類器多次短暫離線，未驗證期間未 commit（守「綠燈才 commit」）；分類器恢復後一次驗證 + commit。

## 11:29 +0800 — 增量9 監控特徵子集選擇 + 精確 X 欄名（c4094c4）
回應使用者「顯示各資料集精確 X 欄名 + 開放只監控其中子集（例如 10 取 7）」。先實跑 registry.build 取真實欄名
（steel_covert=lag_react/lead_react/lag_pf/lead_pf、ccpp=AT/V/AP/RH、synthetic=x00–x09、tep=xmeas01–22、uci=s000–127）。

落地：
- 後端 build_and_save_model / build_model_for_process / acceptance_summary / acceptance_from_dataset 接 `features`
  子集（≥2，保原欄序）；建模/驗收/軟測量同步用子集。**關鍵**：score_timeline 的 FrameSource 改用
  bundle.x_columns（子集）非 gt.x_columns（全集）→ 子集模型維度對齊不崩。build 回傳加 x_columns。
- 治理 gate 拆分（Rule 7）：FPR 過高＝誤報→硬擋；recall 偏低＝監控子集丟訊號參數的知情靈敏度取捨→警告不擋。
  實證：synthetic 10→8 recall 掉但 FPR 合格→存檔+橙色警告；steel_covert 某 3-subset FPR 過高→硬擋。
- 前端 step1：精確 X 欄名 Checklist（預設全選、可勾子集 ≥2）+ 已選 N/M 摘要；成功訊息顯監控參數；recall 低警告。

驗證：test_demo（子集建模/評分維度對齊/最少2/欄名外露）+ test_assets_demo（recall 警告不擋）+ test_acceptance
全通過（17+6 passed）；demo_app import 27 callbacks；synthetic-8 _build saved+警告、steel_covert 子集 FPR 擋 smoke 通過。

## 12:13 +0800 — 根因修復：驗收吃使用者選的 golden（c3ac9e0）
使用者試 steel_covert/tep_tp 建模恆 FAIL（FPR 過高）。診斷：acceptance 用固定 golden_mask、無視精靈選的 golden →
季節性/時序資料（steel 15min 級日週期、tep_tp 第一段 A 內含非平穩區 600–2400）的真值段含非平穩尾 → 恆 FPR-FAIL，
golden 選擇對 gate 形同裝飾。修復：acceptance_from_dataset/acceptance_summary/build_model_for_process 接 golden 規格，
在使用者實際 golden 段上評 FPR/recall（向後相容 golden=None 用真值段）。實證 tep_tp：0:3000 win60 FPR0.12 FAIL；
0:1200 或「自動挑乾淨段」→ FPR0、recall1.0 完全 PASS。測試修正：原機制測試的 golden=(0,600) 跨 A+B（爛 golden，舊
行為忽略才看似沒事）→ 改乾淨 (0,300)；新增 test_acceptance_reflects_user_golden。17+6 passed、import 27 callbacks。

## 12:xx +0800 — 白話演算法導讀（子代理）
派 general-purpose 子代理讀 8 個實作檔（mspc/drift/soft_sensor/health/y_health/dqi_x/config/interface）+ batch_dtw，
寫 docs/algorithms_plain_guide.md（349 行，給化工人）：解什麼問題/判斷鏈 L1–L5/核心統計量(T²/SPE/GSI/RBC/軟測量/
Wasserstein-KS-MMD/DTW)/模型建立/兩種飄移/Health Index 融合。文獻只引 literature_crossref.md VERIFIED 者；RBC
(Alcala & Qin 2009) 不在主表 → 標 (文獻待查) 不捏造。待辦：RBC 回填查證、可派紅隊審導讀比喻/公式忠實度。
