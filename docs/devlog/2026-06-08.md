# Devlog 2026-06-08

## 14:15:15 +0800 — 續推①② block-aware L2 FWER + TEP 保時序前端（merge 71537ff）[verified]

### 背景
選配②收尾列兩項「下一步」，使用者要求完成。皆走 worktree 隔離 + verify-first + ≥2 獨立紅隊複審。

### ① block-aware L2 FWER（910ea5d）
- **問題**：②時序情境暴露——FWER L2 用 permutation 兩樣本假逐樣本可交換；自相關連續窗 SPE 有效樣本
  數 ≪ 窗長 → in-sample 過度拒絕（實測 FP@.05=0.22≫α）。
- **修正**（`health.py`，比照 ② L4 window-vs-window）：自相關 golden（`drift_.block_len_>1`）時 L2 改
  `_block_window_pvalue`（null=golden **連續窗** SPE 均值，自校準），`_fwer_mspc_` fit 保序全 golden；
  iid（synthetic）維 permutation（向後相容，紅隊驗證逐位元一致）。
- **驗證**：in-sample FP@.05 0.22→0.050（名目）、family fwer_alarm FP=0.025（≤α）；drift 功效保留
  （L2 p=0.005）；mutant（強制 permutation）FP=0.12 證修正必要。

### ② TEP 保時序前端（8c6a100 + 紅隊修正 8469021）
- `server.py`：新增 dataset `tep_tp`（路由 `time_preserving=True`）；新增 `/fwer` 端點（per-campaign
  AC-6 `fwer_alarm` + `rejected` 層；獨立端點不拖慢 /analyze 熱路徑）。
- `schemas.py`：`FwerCampaign`/`FwerResponse`。
- `frontend`：dropdown 增「TEP（保時序）」；⑧ AC-6 嚴格告警圖（紅=告警標層、綠=正常）+ 白話 caption。
- **誠實呈現**：保時序 drift 的融合 HI=0.85（>0.6 不過閾），偵測走 AC-6/L2——caption 明示「以本圖為準、
  非只看①」，避免「分數沒掉=沒抓到」誤讀。`/fwer tep_tp` 經 HTTP：golden/clean=False、drift=True 且
  `rejected=['L2']`。
- **紅隊修正**：fwer fetch 記憶化（消 tep_tp 每次下拉 ~80s 重算）；`build_fwer_figure` pvalues 防禦取值。

### 變更檔案
- `src/health_index/health.py`（① block-aware L2）
- `src/health_index/api/server.py`、`schemas.py`（② tep_tp + /fwer）
- `frontend/app.py`（② ⑧圖 + 記憶化）
- `tests/test_fwer.py`（① block FP/power/向後相容）、`test_api.py`（/fwer + tep_tp）、`test_frontend.py`（⑧圖）

### 驗證結果
| 項目 | 結果 |
|---|---|
| ① golden FWER（紅隊實測） | L2 FP 0.041–0.073、family 0.027（≤α=0.05）；mutant 0.12 |
| ① 向後相容 | synthetic permutation 路徑逐位元一致 |
| ② /fwer tep_tp | golden/clean=False、drift=True `rejected=['L2']`、B/C=True |
| ② 誠實前提 | drift /analyze HI=0.849、is_alarm=False（融合確實不旗標）→ ⑧圖 L2 亮紅 |
| 紅隊複審 | ≥2 獨立視角（FWER 嚴謹 / 整合誠信），verdict SHIP，無 BLOCKER |
| 全套件（main） | 177 passed |

### Wisdom 雷區
- **block-aware 是 ② 的自然延伸**：L2 與 L4 同病（permutation/vs-pool 假 iid → 自相關下誤報），同藥
  （window-vs-window 自校準）。`_fwer_l2_block_` gate 確保 iid 路徑完全不變。
- **`_block_window_pvalue` 是篩選統計量**（obs 均值 vs 單對 null），非嚴格 uniform p；紅隊實測型一近名目
  （AR(1) 0.060±0.014），決策分離極大故穩健。s≥golden 長度時退化為保守（p=1，不誤報）——實務不觸發。
- **誠實呈現＞炫目**：保時序 drift 融合分數不掉，硬要讓它掉=造假。改加一張 AC-6 圖 + caption 說清楚
  「看哪張、為什麼」，比動門檻誠實。
- **重端點要記憶化**：/fwer permutation ~80s，Dash callback 任一 Input 變動就整段重算；closure dict
  memo 是最小修法（紅隊 B 抓到）。

### 下一步
- `_estimate_block_len` 在強非平穩段可能逼近 n//4 cap（紅隊 A NIT）——L4 power 潛在脆弱，列觀察。
- 真實有延遲產線 adapter 接入時補 d>0 預測域對齊（沿用③）。

## 13:07:57 +0800 — 選配② L4 block-bootstrap 控制限重構（merge aa4aeba / feat 725d704 / docs 354ded3）[verified]

### 背景與決策
使用者拍板「承擔 block-bootstrap 控制限重構」（②先前判為不可行）。高風險跨層改動 → worktree 隔離
（`worktree-l4-block-bootstrap`）、計畫→確認→執行、≥2 獨立紅隊複審後合併。

### 第一性原理診斷（verify-first，避免過度設計）
逐層分解「連續乾淨窗誤報」：**L1=1.0、L2=0.98（健康）、L4≈0.02–0.18（崩）** → **僅 L4 誤報**。
原框架「L2+L4 全面重構」過度設計；真因＝L4 的 iid null 對 golden PC1 自相關（ρ_1≈0.92）過敏。
通道實測：關係飄移（保邊際）由 **L2 SPE 殘差空間**抓（drift SPE 3.49 vs clean 1.62），**非 L4**。

### 變更摘要
- **drift.py**：自估區塊長度 ℓ（PC1 自相關連續 3-lag 落白噪帶；iid→1 統計等價，連續 TEP→~40）；
  `wasserstein_magnitude` 與 `mmd_pvalue` 改 **window-vs-window block-bootstrap**（ℓ>1）——null 抽連續窗
  反映窗級漫遊變異，乾淨連續窗不誤報、真分佈位移（B/C）仍抓。
- **health.py**：FWER `_fwer_drift_` 改 fit 於**保序全 golden**（L4 p 本即自校準），維 block-bootstrap 時序。
- **tep.py**：新增 `time_preserving` opt-in（連續不重抽，golden/clean/drift 取自 m1d00 不同連續段）；
  iid 預設不變。
- **config.py**：`drift_block_z=1.96`。

### 變更檔案
- `src/health_index/detectors/drift.py`、`health.py`、`adapters/tep.py`、`config.py`
- `tests/test_drift.py`（block-len 不變式、block-bootstrap 不誤報+抓位移+mutant）、`tests/test_tep.py`
  （保時序 marquee：clean 不誤報/drift 偵測；L2-not-L4 歸因鎖定）

### 驗證結果
| 項目 | 結果 |
|---|---|
| 保時序情境 fwer_alarm | golden=clean=False、drift=True（L2 SPE）、B/C=True ✓ |
| 簡單融合 HI（drift） | 0.846（>0.6 不過閾）→ 偵測經 **fwer_alarm**（誠實標） |
| 情境穩健性 | 12/12 seed×strength（紅隊實測，非 cherry-pick） |
| L4 golden 誤報率 | 0.000（含 in-sample，紅隊實測——保序全 golden 不致 FWER 膨脹） |
| MMD 型一誤差 | ≈6%@.05、1.3%@.01（近名目，B=200 噪內） |
| 全套件 | 155 passed / 2 skipped（indpensim/uci 資料未下載） |
| 紅隊複審 | ≥2 獨立視角（統計嚴謹 / FWER+整合），verdict SHIP，無 BLOCKER |

### Wisdom 雷區
- **iid null 對自相關過敏是 ② 不可行的真因**：window-vs-window（不是 vs-pool）是關鍵——vs-pool 在
  非平穩下對「300 樣本窗 vs 3000 樣本池均值」的自然差異過敏。wasserstein 與 mmd 皆須改 window-vs-window。
- **FWER split 洗牌會毀 block-bootstrap**：`gfit=G[perm[...]]` 打亂時序 → block_len_=1 → 退回 iid null
  → 連續乾淨窗誤報。L4 自校準故可 fit 全保序 golden（紅隊實測不致 FWER 膨脹）。
- **block-len 估計：首次穿越 vs 連續落帶**：非單調 ACF（AR(2) 振盪）會在某 lag 偶然近零 → 首次穿越
  低估 ℓ。改「連續 3-lag 落帶」+「lag-1 即不顯著則 ℓ=1」守 iid（19/20→1）。紅隊 A#2 修正。
- **偵測改由 L2、簡單融合 HI 不過閾**：時序情境下關係飄移是 L2 殘差訊號，融合 HI（權重稀釋）停在
  0.846，須靠 fwer_alarm（AC-6）抓。誠實 surface，非宣稱融合分數會明顯崩。
- **既有債（非本次引入）**：L2 permutation 在 in-sample 自相關窗略不保守（族系誤報~0.08）；held-out
  re-entry 窗受控 0.04–0.05。block-aware L2 列後續。

### 下一步
- block-aware L2 permutation（消 in-sample 自相關不保守）。
- 前端 dataset dropdown 增「TEP（保時序）」選項，呈現 ② 情境。

## 10:30:13 +0800 — 選配③ X→Y 延遲對齊接入 L3 軟測量（commit d7cfe95）[verified]

### 變更摘要
把既有但未接線的 `preprocess/align.py` 延遲對齊機制接進 `/softsensor`：估計 X→Y 延遲 d
→ 以 `delay_align_train` 取 (X(t−d), Y(t)) 對齊訓練 GPR → 回報 `y_delay_steps`，前端 ⑥ 圖標示。

### 變更檔案
- `src/health_index/preprocess/align.py`：新增 `delay_align_train(X, y, d)`（連續 golden 塊不變式）
- `src/health_index/api/server.py`：`/softsensor` 接入延遲估計+對齊；docstring 區分 d=0 兩來源
- `src/health_index/api/schemas.py`：`SoftSensorResponse.y_delay_steps`
- `frontend/app.py`：`build_softsensor_figure` 延遲對齊註記 + 頂部留白
- `src/health_index/adapters/tep.py`：docstring 誠實標「2h 物理延遲無法復原」
- `tests/test_align.py`/`test_api.py`/`test_frontend.py`：對齊配對、留出集泛化、邊界、API 欄、前端註記

### 驗證結果
| 項目 | 結果 |
|---|---|
| 全套件（commit 前） | 167 passed |
| 影響套件（修正後） | 50 passed（align/api/frontend） |
| py_compile（5 檔） | OK |
| 留出集泛化（delay=3） | aligned MAE 0.45 < naive 0.87（≈std(y)1.05） |
| 紅隊複審 | ≥2 獨立視角（統計嚴謹 / 整合誠信），verdict SHIP，無 BLOCKER |

### Wisdom 雷區
- **d=0 的兩種來源不可混淆（Rule 12）**：synthetic 是 estimate_delay 真跑 R² argmax 得 0
  （X、Y 同潛變數、列對齊）；tep 預設稀疏標籤 common obs(29) < 2p+1(45) 低於可靠下限，回**保守
  fallback 0**（非 argmax 證得）。紅隊抓到原 docstring 把 fallback 講成「估計確認無延遲」，已修正。
- **使用者原想要的「2h 物理延遲」在此半合成、列對齊資料上無法復原**——與選配②同根因（iid 重抽/同時刻
  取樣移除時間結構）。誠實 surface，不偽造；對齊機制備妥，真實有延遲產線插入即估出 d>0 並校正。
- **in-sample 殘差會說謊**：GPR(RBF) 會把無關的 naive 對 overfit 到低 in-sample 殘差（naive 0.11 <
  aligned 0.22）。唯**留出集**揭穿錯位映射（naive 退化為預測均值）。WHY 測試改用留出集。
- **預測路徑 d>0 latent 不一致**：訓練 f:X(t−d)→Y(t) 但端點預測 Ŷ(t)=f(X(t))，d>0 時實際估的是
  Y(t+d)。目前 d=0 不觸發，已於 docstring 標記，未來真實延遲資料須對齊預測域。

### 三項選配總結
- ① Y-MSPC 品質分布健康：完成（commit d1b8653）。
- ② 保時間結構 TEP 情境：**驗證為不可行**，需 block-bootstrap 重構 L2/L4 控制限（commit 255edc1
  誠實記錄，不硬湊）。
- ③ X→Y 延遲對齊：本次完成（commit d7cfe95）。

### 下一步
- ② 若要推進，需決策是否承擔 block-bootstrap 控制限重構（大工程）或維持 iid 重抽務實解。
- 真實有延遲的產線 adapter 接入時，補預測域對齊（d>0 路徑）。
