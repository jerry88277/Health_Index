# 2026-07-21 devlog

## 09:37:16 +0800 · c16f354 · 批次生命週期 runtime（backlog #9）

新增 `batch_avm/lifecycle.py`：`BatchSchedule`／`due_phases`（4h CSTR 時刻表）+ `LifecycleState`
（completed／g3_notified，JSON 存載 resume-safe）+ `run_phase`（相位狀態機）。

- **x_monitor**（t≈10min）：僅監看 X 域（T²/SPE），依規格不發信。
- **yhat_middle**（t≈2h）／**yhat_final**（t=4h）：算 Ŷ + 正式 G3 AD，越域→通知，且**同批只發一次**
  （防重複告警／未來不重複觸發 SMTP 扣費）；後續相位仍誠實回報仍越域但不重送。
- **y_measured**（事件驅動）：真實 Y 出爐才判 G1——**Ŷ 相位結構上絕不出 G1**（搶跑＝假訊號，已寫成測試鎖）；
  G1 觸發則接 G2 的 X 歸因併入該封 cause。

**G1×G3 同批＝兩封獨立信**（#8 鎖定決策）：先前已發 G3 者只交叉引用 `co_g3`、不重送；未發過則於
`y_measured` 與 G1 同點共發兩封，不合併成單一裁決。

**誠實標記（Rule 12）**：`x_monitor`／`yhat_middle` 的 X* 來自**部分批**資料，與 golden 全批 X* 分佈上
**不可交換** → T²/SPE 與 AD 門檻對其未嚴格校準，故標 `partial_basis=True` + caveat，屬早期預警而非
已校準告警。要嚴格校準需另建「部分批 X*」相位模型（Rule 2 暫不做，明標侷限）。

隔離（Rule 3）：只依 batch_avm 內部 + `notify`（純組裝），不 import 主 HealthIndex/deploy 告警路徑
（isolation test 綠）。

驗證：8 WHY 測試綠、隔離 5 測試綠、全套 554→**562 passed**（302s）。

下一步：backlog #10 殘差自相關 ARIMA 白化。
