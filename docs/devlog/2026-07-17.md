# 2026-07-17 devlog

## 09:36:55 +0800 · dc826a5 · 多產線健康總覽（北極星，backlog #4）

loop backlog #4。新增 `frontend/fleet.py`（呈現殼）+ demo_app 掛載 6 處
（import／scr-fleet 佔位／nav-fleet 鈕／`_route`／`_show_screen`／`register`）。

**先派 understand workflow**（6 agents 唯讀摸清 nav/screen 註冊、fleet 資料模型、告警史/歸因面、
design 規格、app.py↔demo_app.py 關係），收斂出 2 方案 + 3 個「程式無法自決」分岔，經使用者拍板：

- **一線語意＝既有監控點（方案 B）**：per-line 健康沿用 `demo.assets_overview` 三態燈，
  零資料層改動、不碰被結構測試隔離的 batch_avm（Rule 2/3）；真逐 machine_id rollup 留後續 backlog。
- **即時記錄＝`score_timeline` 離線逐窗**（現成）：待 #7 headless runner 再換真 live 流。

點線就地展開三部分：①線上即時記錄（逐窗健康折線，marker 依 persisted_alarm 上色）
②告警歷史（`IncidentStore.list(product)`，每筆下鑽 top_cause＝偏移的 X 參數/Y 量測——本畫面存在的理由）
③模型建立資訊（`model_history` 版本/驗收/稽核）。純資料函數 `fleet_overview`／`line_detail_data`／
`realtime_figure` 可測（line-scoped 不串線、placeholder 誠實標 unavailable、drift 段健康<golden 段）。

驗證：8 fleet WHY 測試綠、全套 526→**534 passed**（395s）；server 開機 GET/ 200、_dash-layout 含
scr-fleet/nav-fleet/fleet-metrics、callback 註冊無衝突。**互動點擊渲染 NOT VERIFIED-visual**
——此環境瀏覽器無法驅動 Dash clientside（對照既有「事件」nav 同樣不切屏，故非 regression）。

下一步：backlog #5 CV harness（nested/repeated CV 模型比較 + conformal coverage，新模組非 crossval.py）。

## 10:00:55 +0800 · 7388271 · CV harness（backlog #5）

新增 `validation/cv_harness.py`（與 `crossval.py` 分工：後者＝主 HealthIndex AC-1/2/3 驗收 runner；
本 harness＝映射模型 GPR/PLS 誰更準 + CV+ 帶是否真達覆蓋，皆離線 out-of-fold）：
`repeated_kfold_score`／`compare_models`／`conformal_coverage`（外層 K-fold held-out 稽核、底線誠實
1−2α 非 1−α）／`nested_cv_selection`（無偏估選擇程序、不被 in-sample 完美的過擬合模型騙）／`format_report`。

**先 TDD 綠、再派獨立紅隊（3 視角：洩漏/coverage/確定性 + 主審對帳）**——抓到一個我漏掉的 no-op 缺陷：
舊版 `repeats` 用 offset 旋轉只把 fold 標籤循環重命名、分割（按 residue class mod k）對 offset 不變＝
no-op → `rmse_std` 結構性恆 0（卻標為穩定度）、`n_eval` 虛增 ×repeats（重複計數同批點，違 Rule 12）。
裁決 fix-then-ship。修法：`repeats` 改用確定性乘法雜湊排列（Knuth-style，無 RNG，Rule 5）產生**真不同
折分割**，`rmse_std` 變真實「對折分配敏感度」、`n_eval` 回歸 distinct 點數 n。裁決建議刪 repeats，
我改採「讓 repeats 真有效」以忠實交付 backlog 的 repeated CV（並加 no-op 回歸鎖測試）。

驗證：7 WHY 測試綠、全套 534→**541 passed**（382s）。紅隊 3 視角有 1 個因 API 中斷失敗，但 coverage
視角已獨立重推確認 leave-fold-out 無洩漏、determinism 視角覆蓋邊界，≥2 視角成立。

下一步：backlog #6 Barber 2021 + redteam_citations §4b 5 筆 DOI 入 literature_crossref.md。

## 10:06:29 +0800 · dd8d85f · 文獻 crossref 轉錄（backlog #6）

把 Barber 2021 + `redteam_citations.md §4b` 5 筆 DOI 轉錄進 `literature_crossref.md`（唯一引用真相）。
**依 Rule 12 + 審核獨立性，落地前逐筆重查 CrossRef API primary source**（不憑 2026-06-02 既有 ledger）：

- Barber, Candès, Ramdas & Tibshirani 2021「Predictive inference with the jackknife+」Ann. Statist.
  49(1):486–507，DOI 10.1214/20-AOS1965（CV+/jackknife+ ≥1−2α 底線出處）——CrossRef 未收錄頁碼 →
  另經 **Project Euclid 出版商頁**確認 486–507。
- Zhang&Zhou 2025 IEEE T-II 21(5):3676–3685（10.1109/TII.2025.3529920，補全卷期）
- Wang 2022 CES 258:117753｜Zhou 2023 EAAI 126:106847（期刊 EAAI，非 audit 誤植 CEP）｜
  Ji 2024 CILS 252:105189｜Guan 2023 ISA Trans. 143:231–243（通用 MTS→標類比）。

`conformal_cv.py` docstring NOT VERIFIED→VERIFIED；`redteam_citations.md` §11 + `batch_avm_design.md`
§11/backlog 同步。零捏造、零 NOT FOUND。20 conformal 相關測試綠、py_compile OK。

下一步：backlog #7 即時 headless runner（batch-AVM 排程跑、save/load state）。

## 10:16:05 +0800 · 37d8d38 · batch-AVM headless runner（backlog #7）

新增 `batch_avm/runner.py`，**比照 `deploy/runner.py` 的 D2 決策**（純函式 `poll_batches` +
外部排程器驅動、非 daemon）：`BatchRunnerState(cursor, n_alarms)` + save/load_state（JSON 重啟安全）；
`poll_batches` 增量處理 `Xstar_all[cursor:]` → 每批 `BatchRunResult`（yhat/band/anomaly/g3_ad_alarm/
g3_ad_top/rbc_top），**冪等於 cursor**（重入不重處理已發批→避免重複告警／未來不重複觸發 SMTP 扣費）、
resume-safe。範圍到 X*→Ŷ + 正式 G3 AD；Y 側 G1/G2 與批內 4h 生命週期屬 #9。隔離：只依
`batch_avm.mapping`、不 import 主 HealthIndex/deploy（isolation test 綠）。

驗證：6 WHY 測試綠（含 resume 與一次跑完 allclose 等價——不同 predict shape 的 BLAS 捨入差 ~1e-10，
非 runner 不確定；同 shape 的 determinism 精確相等），全套 541→**547 passed**（305s）。

下一步：backlog #8 G1×G3 同窗共發（兩封信 + 優先規則）。
