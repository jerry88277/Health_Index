# Devlog 2026-06-15

## 12:35 +0800 — P1.5 窗級標準化調查 NOT WARRANTED + deploy 使用指南 + acceptance verdict

### P1.5 HI-leg 窗級 block-aware 標準化 — investigated → NOT WARRANTED
紅隊指 per-sample σ 在自相關下偏樂觀 2.2×，原型試「窗均值對 golden 窗均值分布標準化」修之。**原型決定性
失敗**：σ_window 極小→門檻剃刀薄→hold-out golden FPR **爆炸**（synthetic 0→0.50、tep_tp 0.12→0.44，
重蹈桶5 剃刀薄門檻）。**現行 per-sample 雖理論偏樂觀、實證更穩健（FPR 0 vs 0.50）→ 維持不動**。
自相關 HI-leg 殘留偏差屬可接受次要債（fwer 為權威軌）。誠實負面發現（免未來重試錯方向）。

### acceptance.verdict()（紅隊 B#3）
passed=False 逐判準說明：FPR 失敗(校準/平穩性) vs recall 失敗(偵測力) vs SPC，避免誤判整體爛。+1 測試。

### deploy 使用指南（docs/deploy_guide.md，onboarding 缺口）
架構審查早先 flag「無 README/onboarding」。新增交付系統使用指南：5 分鐘程式快速上手（4 步）、UI demo、
runner 排程、告警雙視圖、驗收+生命週期、PI 換真酒 checklist、已知限制。**指南內 15 個 API 簽名實測相符**（Rule 12）。

### 交付總結
使用者完整 ask（G1-G4 + demo + P1 + P2）全部交付並驗證（310+ 測試）。剩 PISource（現場 NOT VERIFIED）
+ P1.5(c) is_alarm 統一（低優先，runner 已 union）。autocorrelation 窗級 covert drift 偵測力為已知研究級邊界。

---

## 12:27:06 +0800 — G2b 告警雙視圖（操作員/工程師）[verified]

### 規劃
使用者答覆3：告警同步現場操作員 + 工程師（操作員再人工通報＝第二層防護）。完成 plan 中 [~] 的 G2b。

### 開發（純加法 deploy/alarms.py）
- 單一 AlarmEvent 兩渲染：operator_view（紅綠燈 + 去查哪裡 top RBC 變數 + 持續窗數，零術語）；
  engineer_view（分層語義 L1 資料效度/L2 關係偏移/L4 操作點移動 + p-value + RBC 全排行 + 模型版本）。
- build_alarm_event（從 runner WindowScore + 窗資料組裝，RBC 肇因定位）；AlarmSink 協定 +
  ConsoleSink/FileSink + dispatch（only_persisted 預設濾單窗毛刺）。

### 測試／驗證
| 項目 | 結果 |
|---|---|
| 操作員零術語 | 可證偽斷言無 SPE/T²/p-value/FWER/MMD/Wasserstein |
| 工程師視圖 | 分層 + RBC 全榜 + 版本；可序列化 |
| dispatch | 只送持續告警；雙 sink |
| 全套件 | **310 passed**（+6 G2b）|

### 交付狀態
**使用者完整 ask 全部交付**：G1 bundle / G2a runner+sources / G2b 告警雙視圖 / G3 驗收 / G4 生命週期 /
4 步 demo UI / P1 融合不飽和 / P2 FWER 連續 split。剩 P1.5（深度自相關，研究級）+ PISource（現場）。

---

## 12:19:55 +0800 — P2 FWER 時間連續 split（merge ...，≥2 紅隊 FIX-FIRST）[verified]

### 規劃
G3 harness 實測 tep_tp hold-out golden fwer FPR=**0.44**（synthetic iid=0）。診斷分解：fwer leg 主導
（0.44 vs HI 0.12）、非平穩僅 0.19（<0.3 非主因）→ P2（fwer block 路徑 in-sample 樂觀）是對的高影響修法。

### 開發 + ≥2 紅隊對抗複審（統計/整合）→ FIX-FIRST
- block 路徑改**時間連續 split**：fit 前 2/3 連續段（保自相關）、L1/L2 null 取後 1/3 out-of-sample。
- **紅隊 A#4（L4 regression）**：P2 原把 L4 也 fit 前 2/3 → 對非平穩後段誤報 0.04→0.12 → **L4 改回 fit
  全 golden**（window-vs-window 自校準不需 split）。
- **紅隊匯聚關鍵真相**：P2「→≤α」**單獨結構上達不到**——殘留由 L4 + **非平穩 golden**（桶5 §3.3）主導，
  非 L2 校準 bug。L2 確實修好 **0.44→0.04**。
- **F(Rule 9)**：P2 原零測試（revert 全綠）→ 補 5 回歸（block out-of-sample cal mutant-kill / L4 fit 全
  golden mutant-kill / iid 相容 / block fallback / TEP-gated L2 FPR）。
- **F(Rule 12)**：原計畫「0.17→≤α」stale 且不實 → 更正 plan + fwer docstring（L4「0.000」僅平穩成立）。

### 驗證
| 項目 | 結果 |
|---|---|
| L2 in-sample 樂觀 | 0.44→0.04（真正修好）|
| L4 regression | 修正（0.12→0.08；production full golden ≈0.04）|
| iid 路徑 | 逐位元相容 |
| benchmark full-segment | 全過（drift 仍 fwer-caught）|
| 全套件 | **304 passed**（+5 P2 回歸）|

### Wisdom 雷區
- **修對方向≠修對元兇**：P2 對 L2（0.44→0.04），但整體殘留由 L4 + 非平穩 golden 主導——紅隊揪出「P2 單獨達不到 ≤α」。
- **過度敏感製造假 recall**：P2 前 window recall 1.0 是 golden FPR 0.44 的假象；正確校準後揭露弱 drift window 級真實偵測力低（需 full-segment/持久化）。
- **acceptance gate 正在做事**：50% hold-out 的 0.08 殘留是非平穩 golden 訊號被 surface，非 bug——資料品質問題該被看見。
- **零測試的修＝沒修**（Rule 9）：自相關路徑原無 hold-out FPR 鎖，revert 全綠 → 補 mutant-kill。

---

## 11:45:09 +0800 — G3 生產驗收報告 + G4 模型生命週期（a3450cd, ...）[verified]

### 規劃
完成使用者要的 G1–G4 交付骨架最後兩塊：G3 部署前 hold-out gate、G4 per-product 模型庫+重建。皆純加法
（新 deploy 模組，不動既有 code，Rule 3），develop on main + TDD（含可證偽測試）。

### 開發
- **G3 `deploy/acceptance.py`**：AcceptanceReport（fpr_ok/recall_ok/spc_blind/passed）+ acceptance_report +
  acceptance_from_dataset（時間連續 hold-out split 保自相關/避非平穩假象）。用實際窗級告警率量測（不靠
  per-sample 假設）→ 誠實反映 P1 在自相關下真實 FPR/recall。SPC 盲用相對 golden 底噪增量（高維安全，桶3b）。
- **G4 `deploy/lifecycle.py`**：ModelRegistry（目錄式 per-product，重用 bundle 指紋 verify）+
  assess_model_currency（近期確認-正常資料判不健康→建議重建，需人決）+ rebuild_model（高風險須人觸發）。

### 測試／驗證
| 項目 | 結果 |
|---|---|
| G3 | 5 passed（好模型 PASS；FPR gate 可證偽：drift 冒充 golden→FPR爆→擋；recall gate 抓盲模型）|
| G4 | 5 passed（registry CRUD+fail-loud；CURRENT/REBUILD_RECOMMENDED 可證偽；重建替換；短資料誠實）|
| 全套件 | **299 passed** |

### Wisdom 雷區
- **gate 必須可證偽**：G3/G4 的測試刻意餵壞輸入（drift 冒充 golden、脫節基準）證明 gate 會擋，非 rubber-stamp。
- **時效評估誠實標**：無法自動區分基準老化 vs 真飄移→輸出建議需人決、輸入須現場確認正常（不自動重建避免把問題基準化）。
- **G3 是 P2 的量尺**：acceptance 的 hold-out FPR（compute_fwer=True）正好量測 P2 要修的 fwer in-sample 樂觀。

### 交付狀態
使用者要求的 **G1–G4 + 4步 demo UI + P1 正確性全部完成**。剩 P2（fwer in-sample 樂觀）、P1.5（自相關窗級標準化）、PISource（現場）為 Phase 2 續硬化。

---

## 11:38:55 +0800 — P1 融合層不飽和嚴重度 + 桶5 重開（merge 10e14b8）[verified]

### 規劃
審查發現融合層 L1/L2 子分數＝超限比例（二值化），弱隱性飄移下飽和→HI 漏報。P1 改 per-sample 對 golden
標準化嚴重度 exp(−z/scale)（不飽和、窗長不變、三層一致）。原型驗證 ds=0.3 drift HI 0.628→0.543 抓到。

### 開發（commit 3078837）
- health.fit 存 golden per-sample DQI/SPE baseline；subscores 用 _severity_health；config.fusion_severity_scale=3.0。
- localized：只動 health.py + config（不碰 detectors，Rule 3）。

### ≥2 紅隊對抗複審（統計 / 整合）→ 皆 FIX-FIRST，已修
- **紅隊揪錯（誠實重述 P1 真實價值）**：synthetic 上舊 L2 其實未飽和 → P1 增量**主在 L1 去飽和**
  （舊 inlier 比例對 sub-threshold 域偏移恆=1.0 謊稱健康；P1 severity 隨偏移單調）+ 三層語義一致。
- **F1(Rule 9)**：P1 原零測試保護（revert 全綠）→ 補 5 回歸測試（L1 去飽和 mutant-kill：+0.6σ 舊比例=1.0
  但 P1 severity 顯著低；單調性；窗長不變；DoD 不破壞）。
- **F2(Rule 12, 桶5 重開)**：P1 後 golden HI 0.98→0.93、dead-zone 變窄（真實 indpensim 窗 floor 逼近 0.6）
  → 更新 decision_threshold_calibration.md banner + 哨兵 docstring；結論不變（固定 0.6 仍可移植 FPR≈0）但哨兵價值上升。
- **F3(Rule 12)**：誠實標自相關使 per-sample σ 低估窗級變異 **2.2×** → P1 融合對真實 TEP covert drift 窗級
  recall≈0、由 fwer leg(runner union)補；in-sample 樂觀 n/p 下限；scale 為敏感度非 FPR 旋鈕。
- **F5**：fwer 權威由 runner union 承載（is_alarm 維持便宜快路徑，統一列 P1.5 後續桶）。

### 驗證
| 項目 | 結果 |
|---|---|
| golden FPR（全 seed/含高維 pgn）| 0.00 |
| 弱 drift recall | ds=0.5 漏報→0.76、ds=1.2→1.00（ds=0.3 由 fwer 補）|
| benchmark DoD / 桶5 哨兵 | 通過 / 不誤警 |
| 全套件 | **289 passed**（+5 P1 回歸）|

### Wisdom 雷區
- **紅隊把 P1 價值講誠實**：原以為修 L2 飽和，實證 synthetic L2 未飽和→真正修的是 L1 去飽和；不誇大。
- **零測試保護的修＝沒修**（Rule 9）：能 revert 後全綠的改動必須補 mutant-kill 測試。
- **自相關是連續製程核心域的硬邊界**：per-sample σ 標準化在自相關下 anti-conservative 2.2×，covert drift
  窗級鑑別力靠 fwer；徹底修需窗級 block-aware 標準化（列 P1.5）。

---

## 10:27:32 +0800 — 線上模擬瓶子 DEMO-READY（交付 Phase 1 完成）[verified]

### 規劃
使用者確認交付形態：PI 資料源（軟量測延遲到達、被動分析）、節拍 takt/10、告警同步操作員+工程師、
單一產品一模型、G1–G4 一次來。範圍決策＝**瓶子優先**：公開資料集線上模擬 + 4 步 UI，PI 保留 stub。
計畫版本化於 `docs/deployment_plan.md`（commit 5c84129）。

### 開發（本日 commit 鏈）
- 桶0 golden='auto' 收尾 merge（aca5f0a）——無 label 資料挑最早乾淨平穩段。
- G1 `deploy/bundle.py`（394b883）：joblib + 指紋重放（版本漂移/損毀拒載，D1）。
- G2a `deploy/sources.py`+`runner.py`（e1ff4d4）：DataSource 協定 + FrameSource + PISource stub(NOT VERIFIED)；
  poll_once 純函式 + 狀態 JSON 持久化（resume-safe）+ persistence_k 接線 + is_alarm∨fwer_alarm 聯集（D2）。
- G2-UI `deploy/demo.py`+`frontend/demo_app.py`（c78aa7a）：4 步 orchestration（可測）+ 新 Dash wizard 殼。

### 測試／驗證
| 項目 | 結果 |
|---|---|
| 全套件 | **284 passed**（+21：G1 7/G2a 8/demo 6）|
| 重放時間線（synthetic）| golden HI~0.98 不告警 / 換產品·殘留飄移 HI~0.25–0.28 告警 / **乾淨回歸 A HI~0.96 不誤報**（分離>0.3）|
| Dash demo 啟動 | HTTP 200 渲染正常（視覺未渲染驗證 NOT VERIFIED-visual，邏輯由 test_demo 保證）|
| bundle 指紋 | 漂移/損毀/非bundle 皆拒載 |

### Demo DoD 達成（docs/deployment_plan.md §7）
四步端到端跑通：選定資料範圍 → 建立模型（bundle 存檔+指紋）→ 確認模擬資料 → 查看健康指標時間線。
三條業務 DoD 在時間線上同時成立（golden 健康 / 隱性飄移早於 SPC 告警 / 區分乾淨回歸 vs 殘留飄移）。

### Wisdom 雷區
- **瓶子與酒分離奏效**：UI 邏輯抽到 deploy/demo.py（可測），Dash 殼零演算法——換 UI/換 PI 不動核心。
- **指紋重放 > 格式相容**：凍結模型對同輸入給不同答案就拒載，比版本鎖更根本（Rule 12）。
- **PISource 誠實 stub**：協定隔離使未驗證表面僅限一個檔；本機用公開資料集完整測 runner/告警。
- **demo 用強 drift 情境**：current scoring（is_alarm）對強 campaign drift 有效；弱隱性飄移仍待 P1 修
  （融合層二值化漏報，列 Phase 2 交付前置）。

### 停 loop（依 cron 7a1cf2af 自訂停止條件）
demo-ready 達成 → CronDelete 停止自主迴圈。Phase 2（P1/P2 正確性 + G3 驗收 + G4 生命週期 + PISource
現場填實）為交付硬化項，列下一步待使用者授權續推。
