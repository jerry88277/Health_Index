# Devlog 2026-07-03

（承 2026-07-02：全專案對抗式風險稽核 + 使用者敲定 G1/G2/G3 監控語意與批次生命週期；本日起建新精靈 batch-AVM 管線。git 時間為權威。）

## 背景（2026-07-02 決策落地方向）
使用者補 3 目標（各以 SMTP 收尾）+ 4h CSTR 批次生命週期（10min 起監 X → 2h Ŷ_middle → 4h Ŷ_final → 出 Y 比歷史查 G1 隱性漂移 + G2 參數歸因）。對抗式風險稽核（wf_8bac24a4，10 agents）抓出 13 個 project-kill 風險：SMTP/歸因/G1-純Y/G3-適用域**整個交付半邊不存在**、數值奇異→錯歸因信、跨線多重比較、無 headless 即時、G1 在 TEP 結構性不可證等。使用者定調：G1 用**合成儀器漂移資料**、G1×G3 **兩封信**、G1 **獨立輕量模組**、SMTP/誤報**暫緩**、**先把新精靈管線串起來**。決策存 memory（project-core / golden-wizard）。

## 10:06 +0800 — 批次疊圖 + [param×stat] 指標轉換（5052b8a，新精靈 INC-1，[verified]）
新 `preprocess/batch_features.py`（純函數、隔離於主 HealthIndex，僅依 numpy/pandas）：
- `batch_temporal_overlay`：多批 temporal 疊圖，各批**同法 trim**；`resample_n` 時另回共同進度格的
  逐點 median + [band_q, 1−band_q] 分位帶（**display-only**，先算 count 再 resample）。
- `batch_indicator_matrix`：每批 `[param×stat]` **X\***（映射模型輸入）；`count` 走**原生格**反映真實
  批長（餵 DQIx）、`cv` 有 `|mean|` floor 防除零、`min/max/range` 為極值統計（跨批長度偏差留 UI/模型層
  n-guard，設計 §4）。
- TDD 紅→綠：6 WHY 測試（原生 count/cv floor/trim/疊圖帶排序/確定性/未知統計 fail-loud）；全套 **451
  passed（+6）**、`py_compile -W error::SyntaxWarning` 乾淨。

## 10:06 +0800 — 更正 batch-AVM 設計文件 §11 stale 註記（3bffb31）
實地查證更正兩處：(1) `data/tep/` 12 個真實 MMFDD-TEP `.mat`、`tep.generate()` 本環境可跑（實測 300 列/
22 感測器/稀疏 Y）——先前「無 TEP .mat、未實測」為誤述（workflow 紅隊在隔離環境假設資料不存在，未查證
沿用）。既有五維鏈對注入型 covert drift 已實測 univ≈0.04、HI 0.55 vs 0.95。(2) DQIy DOI 衝突已解，
crossref 統一 `10.1109/TSM.2011.2146006` VERIFIED。

## 新精靈建置增量（規劃）
INC-1 [x] 批次疊圖 + 指標轉換 → INC-2 X* 上 DQIx/DQIy → INC-3 X*→映射模型(PLS/GPR+CV+) + X* 的
GSI/T²/SPE(fresh 偵測器) → INC-4 批次/機台/時間選取後端 → INC-5+ Dash 9 步精靈前端串接。
（隔離 TDD-3、G1 純 Y 輕量模組、G2/G3 X 歸因、數值護欄等風險項另排；SMTP/誤報暫緩。）

## 13:40 +0800 — 專案盤點 + 全文件對齊 2026-07 方向（2f2fece，[verified]）
使用者指示兩段盤點：(1) 全 repo 不相關/過期檔案→TEMP 待確認；(2) docs 深盤點、Before/After 表核准後修。
- **檔案盤點**：`data/data/`（2.5GB 嵌套重複，零引用、byte 級相同、gitignored）+ `ccpp Readme.txt~` 移入
  `TEMP/`（附 README 審核清單，待使用者手動刪除）；`.gitignore` 加 `TEMP/`。src 45 模組無孤兒檔。
- **docs 深盤點**（workflow wf_37f5d341，10 組稽核員逐檔讀 + code 查證）：31 份判定 28 update / 3 keep
  （model_registry_design、literature_crossref、decision_threshold_calibration）/ 0 可丟——問題是內容過期
  非檔案無用。141 筆 Before/After 經使用者核准（全修；CLAUDE.md 4 筆同意；ledger 只加註；mindmap 5 筆
  延後至 9 步精靈落地後 v3）。
- **批次套用**（workflow wf_12ba135f，28 agent 一檔一員）：145 筆全落地、0 failed；adapted 皆為機械調適
  （插入 vs 取代、保表格/清單格式、ledger 加註規則）。CLAUDE.md diff 驗證恰好 4 筆。
- 意外收穫：redteam_citations §4b 有 5 筆已查定 DOI 從未入 crossref（列入 crossref TODO）；
  modernization_L2 兩筆錯 DOI 更正（.105165→.105189、.09.004→.09.002）。
- 全套 451 passed（docs-only 零影響）。下一步：9 步精靈落地（INC-2 起）。

## 13:57 +0800 — TDD-3 隔離測試 + INC-2 品質視圖（de6d73c，[verified]）
- **TDD-3**：`tests/test_batch_avm_isolation.py`（5 測試）——主路徑原始碼＋子行程 import graph 雙重鎖
  batch-AVM token；純度改 AST 驗 import（初版 token 掃描誤傷 docstring 宣告不變式的散文，修正）。
- **INC-2**：`batch_avm/quality.py::batch_quality_view`（7 WHY 測試）——精靈第 6 關「建模前」後端：
  fresh DQIxGate on X*＋n 一致性閘＋Y 確定性准入閘（取代 ART2 DQIy）；未量測≠正常。
- 全套 **463 passed**（451→463）。下一步 INC-3：X*→映射模型（PLS 主力+CV+）＋ X* 的 GSI/T²/SPE
  （fresh 偵測器 + highdim 預投影）。
