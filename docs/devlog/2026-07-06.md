# 2026-07-06 devlog

## 16:02:34 +0800 · 29128c4 · 正式 G3 適用域（AD）

loop backlog #3。新增 `batch_avm/applicability.py`：`ApplicabilityDomain` +
`fit_applicability` + `applicability_check`。G3＝Ŷ 越出模型認定範圍，兩個**正交**訊號——

- **leverage** h(x)=1/n+x_s^T(Xs^TXs)^+x_s（標準化特徵空間 pinv Gram，容 p≥n）；QSAR 限
  3(rank+1)/n＝X 結構外推。p≥n 時 lev_limit>1、leverage 幾乎不觸發，誠實標
  `leverage_informative=False`（此時 Ŷ-範圍 carry G3）。
- **宣告 Ŷ 有效範圍** [y_min,y_max]（golden y±5%）＝響應空間外推——**T²/SPE 完全偵測不到**
  （X 落合理域內卻預測到訓練 Y 範圍外，這正是 G3 存在的理由）。

歸因走 leverage 逐特徵貢獻→param。fit 時 golden y 範圍退化→raise（Rule 12 不假評），
mapping try/except→`ad_=None`。wire：`fit_batch_model` 建 `model.ad_`、`score_batches`
每批帶 `g3_ad_alarm/leverage/yhat_in_range/g3_ad_top/g3_ad_reason` + `summary["applicability"]`；
精靈第 9 關 G3 下鑽改用正式 AD（取代 T²/SPE 代理 domain_exit）。

驗證：7 AD WHY 測試綠（in-domain / 結構外推歸因命中 c2 / Ŷ 正交訊號 / QSAR 限 / 退化 fail-loud /
確定性 / wire），全套 519→**526 passed**（489s）。byte-compile OK。design doc §13 #3 標 `[x]`。

下一步：backlog #4 多產線總覽畫面（北極星）＋點線進即時記錄/告警史/模型資訊。
