# Devlog 2026-06-03

## 08:51:34 +0800 · commit `2d86b70` · tag `green-20260603-b4`
**B4 — L5 批次 DTW：IndPenSim 軌跡形變偵測（backlog 收官）[verified]**

- 使用者授權下載 IndPenSim `100_Batches_IndPenSim_V3.csv`（2.57GB，Mendeley npt257bjxn，唯一含批次時序的檔）→ adapter 以 usecols 取前 39 欄避開 ~2200 欄 Raman、快取 `process_only.csv`（21MB，Rule 6）。
- `adapters/indpensim.py`：依 **Time(h) 重置**切 100 批（長度 **835–1450 不一** → DTW 用武之地）；x_columns=25 線上製程變數（排除 offline 稀疏/旗標/Time）；**標籤資料驅動**＝讀 `Fault reference` 欄逐批導出（實測恰標 91–100 faulty、與 survey 結構雙重佐證；不信值異常的 `Fault flag` 欄，Rule 8）。批次模態另立 `BatchDataset`，不套連續 `ProcessDataset` 契約（Rule 3 不破壞連續骨架）。
- `detectors/batch_dtw.py`：`BatchDTW` 降採樣固定點數對齊長度 + Sakoe-Chiba banded DTW + 正常參考批 LOO 距離校準門檻（mean+k·std）。
- **marquee WHY**：正常 hold-out 偏移率 **0.03（不退化）**、faulty(91–100) **0.70 被抓**（10× 正常率）；變長軌跡（835–1450）經 DTW 可比。
- **2 獨立紅隊（方法學 / 資料語意）各抓 1🔴 已修**：
  - 方法學：**DTW 必要性未被測試守護**——`_prep` 先 resample 成等長後，把 DTW 換 Euclidean 仍全綠（甚至更乾淨）→ 補**相位漂移合成對比測試**（尖峰 @0.5 vs @0.6，DTW 距離 17× 小於 Euclidean）鎖死 DTW>Euclidean；docstring 誠實降級：resample 解長度、DTW 加相位 warping 容忍，**IndPenSim 上 Euclidean 亦足、DTW 邊際增益未在此資料證實、為防禦性選擇**。
  - 資料語意：docstring 誤稱「無可信 fault 欄只好靠文件」→ 實有乾淨 `Fault_ref` 欄，改**資料驅動標籤** + 澄清 `Fault flag`(垃圾) vs `Fault reference`(乾淨) 兩欄之別。
  - 採 🟡：門檻 k=2≈5% FPR **僅同質參考塊成立**（ref 11–30→0.17、61–80→0.49）、normal 含 recipe/operator/APC 三模態（參考取純 recipe 1–20）、docstring「~31」→39 欄。
- 成本（Rule 6）：降採樣 100 點 + band 封頂 O(n²)；fit 1.3s、score 0.07s。
- **誠實殘項**：非全域形變的 fault 漏抓（faulty 70% 非 100%）；DTW vs Euclidean 在真實批次的實證增益待更多資料；`BatchDTW` 內層雙迴圈未向量化（3.4ms/次，擴批再優化）。
- 驗證：pytest **142 passed**（B3 為 133，+9 B4）、py_compile OK、L5 marquee 實測。

### 🎯 backlog B1–B4 全部完成
B1 時間軸+RBC 上前端 → B2 真實集 adapter(UCI Gas Drift, AC-4) → B3 AC-6 FWER 單一決策點 → B4 L5 批次 DTW(IndPenSim)。連續型五維度判斷鏈 + 全棧 + 真實集 + 批次型 L5 皆就位；三條成功判準與各承載結論皆有 WHY 測試 + ≥2 獨立紅隊守護。
> **後續可選 backlog**（非 MVP 阻塞）：全鏈 L1/L4 的 PCA-score 降級（p=128 真實高維）、RealSetResult 多 seed 帶、DTW 向量化、更多真實批次集驗證 DTW 增益、維修型 re-entry。

**下一步**：開發迴圈完成，停止 cron。MVP（M0–M10）+ backlog（B1–B4）皆收官。
