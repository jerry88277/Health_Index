# Devlog 2026-07-01

（承 2026-06-30 第二波；本日 commit 跨午夜，git 時間為權威。）

## 08:26 +0800 — 評分窗長存 bundle 單一真相 + 下鑽/匯出口徑一致（5720917，紅隊 A20/A22）
評分窗長原由各消費端各自帶 → 建模與下鑽/匯出可能用不同窗長、匯出對不上螢幕。方案 A（存 bundle，經使用者確認）：
- ModelBundle 加 `window` 欄（預設 None、不入指紋——窗長與 fit 凍結控制限解耦、verify 不受影響）；build_bundle/
  build_and_save_model/build_model_for_process 接並存。
- score_timeline window=None → 讀 bundle.window（getattr 舊 pickle 相容→60）；_score_current 總覽燈 per-bundle window。
- 前端：時間線點 customdata 帶 `[start, win]`（win=bundle.window 掃出的確切窗）；_detail 用該窗非重算（承 A21）；
  _run 傳 window=None；**匯出 callback 不帶 window→讀 bundle.window→與螢幕一致（A22）**。
4 WHY + 前端實呼叫（皆用 bundle.window=90、精靈 60 被忽略）。全套 437。

## 08:37 +0800 — Ŷ 外推漏報揭露 + conformal/persistence 誠實措辭（f7f0456，紅隊 A13/A11）
- **A13**：X 大幅離域（re-entry）時 GPR 外推回 prior mean → Ŷ 趨中、Ŷ-drift z 縮小 → 隱性品質**漏報**（反直覺）。
  以 confidence（域相似度 < y_extrap_confidence_min=0.6）判外推 → 該窗 yhat_drift_reliable=False（不以 z 縮小當
  健康憑據）；score_timeline 加 n_quality_unreliable；前端品質列標「N 窗 Ŷ 外推不可信、改看 X 側」。2 WHY。
  （原試 band 膨脹判外推，改用 confidence——更穩且正合紅隊「confidence 低時不信 z」。）
- **A11（純措辭）**：conformal 覆蓋改「近似、非嚴格保證」（in-sample 校準、GPR 過擬合可能窄於名目；soft_sensor×3
  + UI「近似覆蓋」）；acceptance persistence_k 標驗收 k=1 vs 線上 k≥2 為**設計分工非脫鉤**。

全套 439 passed。第二波我列的 MAJOR 項（A6/A10/A11/A13/A14/A15/A17/A19/A20/A21/A22 + events dedup on main）收尾。
尚存候選：recall gate 統計檢定力 + hard-block option、A5/A7 降採樣漏報下鑽提示、tep2py+IDV13 物理接地飄移、
單點/逐樣本 L2-only 監控模式、RUN_ID 契約文件分歧小債。

## 16:53 +0800 — 安裝 design-advisor + taste-skill 至 project 層級（a0064b2）
兩個第三方 skill 經安全審視（clone 後掃描：無網路呼叫/exec/憑證存取），僅收主 SKILL.md 至 `.claude/skills/`；
taste-skill 另 12 子 skill 與 build 工具未收。用途：batch-AVM 精靈 UI 設計與稽核。

## 16:53 +0800 — 規劃：Golden 兩關卡圖選 + 多機台/時間 + batch-AVM 模型分析（設計，未實作）
本日與使用者敲定一組承載性設計（尚未寫 code；待最終整合紅隊 → 回填設計文件 → TDD）。決策已存 memory
`golden-wizard-redesign.md`。要點：
- **Golden 兩關卡**：關卡1 temporal 疊圖（批內製程時間 0–100%、中間 X% 均勻 trim、批次 CheckBox 納入/剔除、
  中位/離群標示）；關卡2 indicator（`[param×stat]` run 圖 over 批次生產時間，10×8=80 小倍數，惰性計算+%進度、
  前端僅顯示待按鈕）。⚠ 關卡2 為逐變數邊際圖，隱性多變量飄移須由 GSI/T²/SPE(對 X\*) 負責。
- **統計指標集**：現有 6 + count + cv + 自訂 hook。
- **resample**：X\* 走原生格（統計量長度不變、count 先算餵 DQIx）；resample 僅畫圖層（中位/分位帶）；DTW/軌跡級
  預設不開（L5 離線）。
- **多機台/時間**：`machine_id` 選用欄（S-B 向後相容、非偵測器輸入、五維骨架不動 Rule 3）；`generate_fleet`
  seed+系統偏移合成（M-2）；UI 可勾選批次表（U-2）；pooling 異質 Golden 撐寬基準 → 同質性閘警告（非硬擋）
  + 分層模型替代。9 步精靈流程對映 L1→L3。
- **模型綜合分析**（workflow w9dsjha8v，8 agents + 獨立紅隊翻案）：紅隊抓到原建議兩 blocker——(B1) 現況
  soft sensor 吃 raw X(p=10/22)→一律 GPR，`[param×stat]` 層結構性禁入偵測路徑(features.py:5-7)，X\*→模型是
  **未來新路徑**非換現有；(B2) 選型聲稱用「現有 crossval」但該功能不存在。裁決：**GPR+PLS+CP、零新模型**；
  normalized-CP/ElasticNet/kNN 全 SKIP（Rule 2）；未來 batch-AVM 路徑 PLS 主力。NOT VERIFIED：AVM conjecture
  model 家族 crossref NOT FOUND、GPR off-support 變異/RF clamp 未實測。
- **CP vs RI**（crossref 第24行 VERIFIED）：RI=雙模型高斯重疊面積(0-1)、無覆蓋保證、共同盲點；CP=殘差分位區間、
  有限樣本 marginal 覆蓋、模型無關。改 CP 因對準「真值落入機率」且模型無關。
- **CP 小 n 效度修法**：golden 單 campaign n<cp_min_calibration=200 → CP 常退回 GSI；in-sample 校準破
  exchangeability → 區間偏窄。改 **jackknife+/CV+**（每樣本 both fit both 校準、確定性、近似保證），與模型比較
  harness 共用 K-fold。列為**優先於**模型變更。
- 放棄卡死 workflow wv1uz81au（0-byte、UX 再稽核價值被新精靈設計覆蓋、AVM 軌被 w9dsjha8v 覆蓋）。
