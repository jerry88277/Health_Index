# Devlog 2026-06-17

## 09:17 +0800 — 增量7-A 模型 registry AssetStore（0c26e2c）
使用者要求模型管理：手動更換模型 / 查看歷史監控紀錄 / placeholder 熱插拔 / 軟刪除+log；選定「完整 registry
table」。先派 **3 個獨立子代理紅隊**審 schema（狀態機/資料完整性、向後相容/最小性、產品落地），對帳採納：
current 為衍生(砍 active/superseded)、version 單調計數器(刪後不碰撞)、原子寫入(temp+os.replace)、軟刪製程
強制關閉孤兒 incidents、軟刪不刪檔、id ascii-slug(中文不入路徑)、placeholder 不污染健康燈(待後續三態)。
`deploy/assets.py`（AssetStore：create_process/record_build/soft_delete_*/restore_*/history/audit_log）+
12 WHY 測試 + `docs/model_registry_design.md`（紅隊後設計真相）。全套件 377 passed（原 365 +12）。

## 09:23 +0800 — 增量7-B registry orchestration（11aeb5d）
`demo.py` 接 AssetStore：create_process / build_model_for_process（FAIL gate→版本化 bundle→登錄，更換模型＝
單調升版）/ delete_model / delete_process（關孤兒事件）/ restore_process / model_history / assets_overview
（健康燈三態，placeholder 不進綠紅分母）。`build_and_save_model` 簽名不動，既有測試走 glob 不碰 registry。
5 整合 WHY 測試，39 passed（demo+p2+assets）。

## 09:38 +0800 — 增量7-C/7-D 前端接 registry + demo 導引（f739327）
item2：總覽改 assets_overview 三態、製程卡明確按鈕（查看/更換/建立/歷史/刪除）、新建製程 placeholder 表單、
歷史/稽核頁（版本+acceptance 快照+audit log+服役事件+還原）、精靈綁製程、bundle-store 帶 dataset（修解耦後
評分用錯資料源）。item3：`catalog.py` 每資料集說明+推薦 window，精靈 step1 說明卡+套預設，結果頁粗→細標示
（① 健康指標 → ② 最嚴重窗摘要 → ③ 點窗看製程參數）。import OK（24 callbacks）+ callback smoke 全流程通過，
catalog 覆蓋全 9 集，53 passed。
