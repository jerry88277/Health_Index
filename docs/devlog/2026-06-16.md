# Devlog 2026-06-16

## 14:11 +0800 — 產品 UI 重構為三屏（90d790b）
依 design-advisor 自審修 3 問題：①監控總覽 dashboard 首屏（模型紅綠燈 + 新建入口）②新建精靈 5 關 stepper
（第幾關／還剩幾關）③訓練時間範圍改 RangeSlider 時間軸圈選。色彩語義 legend（綠健康／紅告警／藍可信度）
+ 單一 accent 靛藍。結果屏重用既有 timeline/ymap/detail，接 `demo.score_timeline`/`window_detail` 真資料。
驗證：HTTP 200、10 callbacks 無衝突、_build→_run（3+3 traces）→stepper/dataset 直呼真資料通過、test_demo 14 passed。
NOT VERIFIED-visual（視覺/點擊未渲染）。

## 14:05 +0800 — 銷售 hero landing 頁（8ff85a4）
依 taste-skill：Design Read（B2B 工業 SaaS hero）+ dials(VARIANCE4/MOTION3/DENSITY5) + 自訂工業美學。
痛點→解法→真實資料證據（CCPP/Steel：單變數越限≈0.6%、SPE 越限 30–46%、HI 0.94→0.47）→四步→CTA。
pre-flight：em-dash 0、單一 accent、CTA 不換行、無 serif/beige。`frontend/landing.html` 載入 HTTP 200。

## 13:58 +0800 — 落地 design-advisor + taste-skill 方法論（c7adffd）
外部 skill 知識依規範落地為 `docs/frontend_design_guide.md`。**關鍵修正**：taste-skill scope 明確排除
dashboard/multi-step form/data table → 它管「行銷 landing 頁」，產品 UI 走 design-advisor。含 design-advisor
對原型自審（缺銷售 hero、色彩語義宣告、結果頁密度）。來源逐 URL 標、非逐字複製。

## 13:44 +0800 — L3 映射呈現 Ŷ vs 實際 Y 對照子圖（6d7606c）
`score_timeline` 每窗附 yhat_mean + yhat_band(conformal) + y_actual_mean + has_y_mapping 旗標 → 前端可畫
Ŷ vs 實際 Y（帶外＝X→Y 關係偏移，正交於 X 側 HI）。demo_app 加 ymap 子圖。test_demo 14 passed。

## Wisdom 雷區（今日）
- **外部 skill 先查 scope 再套用**：taste-skill 自己排除 dashboard/multi-step form——把它套產品 UI 是用錯範圍。
  正確分工：design-advisor 管產品 UI/UX、taste-skill 管行銷頁。先讀 SKILL.md 才不會誤用（Rule 8）。
- **銷售 demo 是兩個表面**：吸引購買的 hero（taste-skill）+ 可操作產品 UI（design-advisor），別混為一談。
- **Dash 三屏路由**：用單一 ctx router callback 寫 screen store（避免多寫衝突）+ 顯示 callback 切 display；
  stepper 用 wstep store 驅動 panel 顯隱。callback 直呼（非點擊）是本環境唯一可行的行為驗證。

## 14:30 +0800 — 移除銷售頁，直接展示系統前端（使用者更正）
使用者決定不需銷售/行銷 hero，直接展示產品 UI。`git rm frontend/landing.html`；demo_app docstring 去除 landing
引用；frontend_design_guide §4 標銷售頁「不做」、自審問題1（缺 hero）作廢、問題2/3 標已修。產品 UI（demo_app.py
三屏，入口即監控總覽）為唯一前端交付。boot HTTP 200、10 callbacks 不變。taste-skill 知識保留為參考（§3）。

## 15:09 +0800 — 多角色 UX 檢視 + P0 前端優化（74bd6bf）
派 4 子代理（作業員/現場工程師/製程工程師/生產處長）檢視三屏 UI → design-advisor 收斂 3 核心問題 → 實作 P0：
① wall-clock 時間軸（score_timeline 每窗帶 ts；x 軸改時間）② 驗收真接上（demo.acceptance_summary 取代寫死「可上線」，
FAIL 擋）③ 告警可信/存疑判定（window_detail verdict + 下鑽橫幅）④ 總覽各模型當前健康燈（demo.monitoring_overview）。
時間軸改 wall-clock → _detail 改讀 customdata 末位 start（修點擊反推）。test_demo 18 passed、HTTP 200、callback 直呼通過。
P1/P2 roadmap 落地 frontend_design_guide §5。已重啟 8051 伺服器載入 P0。

## 15:40 +0800 — P1/P2 後端 keystone（事件閉環/全廠視圖/ROI/匯出）
使用者授權 P1+P2。後端先（可測）：
- events.py 事件閉環（開案→ACK→關閉+MTTR/stats，同裝置防重複開案）— test_events 7 passed。
- demo.plant_overview（全廠多裝置燈+總燈+各裝置未結事件）、incidents_to_csv/timeline_to_csv 匯出、roi.estimate_roi
  （config 驅動、假設透明，誠實標非實測）— test_p2_backend 4 passed。
伺服器無法跨回合存活（背景被回收）→ 改由使用者本機終端跑 demo_app（持久）。
下一步：前端接線（全廠視圖/事件頁+ACK/MTTR/ROI 卡/匯出鈕/角色切換/聲音/手機）+ 事件產生接點 + 位號 stub。

## 23:00 +0800 — 修兩 bug + 增量5-core 事件閉環前端（a66ca79 + 後續）
- bug #1 ymap 實際 Y 用 sample-index 落時間軸 → 改窗 ts。
- bug #2 uci 卡 LOADING：HealthIndex.score() 單次掃描（消逐窗 3-4× 重算 L4）+ score_timeline 自動降採樣
  （高維 p>64 取 40 窗）→ 340s→23s；synthetic 維持全解析。score() 等價性測試鎖定。
- 增量5-core：demo.ingest_incidents（評分→persisted alarm 開事件，reuse pts 不重複評分）+ event_overview。
  前端：事件頁（KPI open/ack/closed/MTTR + ROI 卡 + 事件清單含 ACK/關閉）、_run 自動 ingest、總覽全廠總燈
  + 各裝置未結事件、匯出 CSV（事件/時間線）、模型卡可點進結果。15 callbacks、HTTP 200、39 tests passed。
- 增量5b 待續：角色切換、聲音告警、手機 responsive、位號對照+DCS 趨勢(stub)。

## 23:xx +0800 — 增量6 P0-a（loop 自動優化）：治理+作業員告警+問責
複審 P0 修正（全在 demo_app.py 前端）：
- 驗收 FAIL **真擋存檔**：_build 改先 acceptance 後 save，FAIL 不落地（修治理漏洞）。
- 自動刷新：dcc.Interval(60s) 接 home-metrics + events-body → 盤面不需手動戳（作業員）。
- 問責：事件頁加「操作者」輸入，ACK/關閉記真實 actor（取代寫死「工程師」；真 auth 為 P2）。
- 處置記錄**綁每張事件卡**（pattern note-inc，依 id 對應）→ 不再共用單一框貼錯案（現場工程師 bug）。
- 下鑽 verdict 加**一行白話 SOP 動作**（現場確認 top RBC 設備/通報，作業員）。
HTTP 200、15 callbacks、test 31 passed。待續 P0/P1：聲音告警、位號對照、門檻 slider、測試段圈選、生命週期、手機。

## 23:yy +0800 — 增量6 P0-b（loop）：誤報排除 + ROI 輸入 + 手機 responsive
- events.close 加 close_reason(real/false_alarm/ignore)；stats MTTR 排除誤報 + 誤報計數；roi 排除誤報 critical
  → MTTR/ROI 不被誤報汙染（現場工程師/處長）。test_events 8 + p2 5 passed。
- 前端：事件卡加 close-reason 下拉（真實/誤報/忽略）；事件頁 ROI 損失可輸入（重算）+ 誤報 KPI；
  index_string 加 viewport meta + @media（.pg-grid 窄屏單欄）→ 手機可看（處長）。HTTP 200、15 callbacks。
待續：聲音告警、位號對照+historian、門檻/persistence slider、測試段圈選、生命週期面板、廠區階層、交接班、PDF。

## 23:zz +0800 — 增量6 P1 位號對照 config（loop）
現場工程師「最後一哩」：RBC 肇因欄名（xmeas07）→ 現場 DCS 位號。demo.tag_map_for 讀
{models_dir}/tags/{product}.json（無則 {} 識別映射，NOT VERIFIED stub 待現場 config）；window_detail tag_map
參數套用到 rbc_ranking + tag_mapped 旗標；前端 _detail 載入並標註「未對照」提示。SOP 動作也顯位號。
test_demo +2 passed、HTTP 200、15 callbacks。待續：門檻 slider、測試段圈選、生命週期、廠區階層、交接班、視覺閃示、role-view。
